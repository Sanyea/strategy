# RBAC 权限管理批 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 补齐 RBAC——权限资源 Java 三件套、注解接口鉴权（JWT perms 快照）、RBAC 管理 API、权限变更自动踢人、数据权限 1/2 级。

**Architecture:** DDD 分层 + 新 rbac 能力包。鉴权零 DB 查询（@RequiresPermission + JWT perms claim 快照，SUPER_ADMIN 直通）；管理写操作 afterCommit 自动 evict（jti 黑名单，TTL 取 token 剩余）；审计独立事务落 ums_oper_log；数据权限方案 C 装配 IWrapper。

**Tech Stack:** Java 21 / Spring Boot 4.1.0 / MyBatis-Plus 3.5.15 (boot4-starter) / Redis (StringRedisTemplate) / Lombok / JUnit5 + Mockito

## Global Constraints

- **DDD 分层单向依赖**：interfaces→application→domain→infrastructure；application/domain 零框架依赖（禁 MyBatis-Plus 类型），DTO 组合分页不继承
- **实体/PO 双层**：实体继承 `SimpleBaseEntity`/`BaseEntity`（纯 POJO），PO 继承 `SimpleBasePO`/`BasePO`（MP 注解），`toPO`/`toEntity` 用 `BeanCopyUtils.copy`
- **物理删除关系表**（ums_user_role / ums_role_permission）：无 deleted，不继承 base，自定义契约接口 + 直连 Mapper（XML 联表 / BaseMapper 写）
- **枚举**：业务枚举 `domain/enums` + `@EnumValue`；`DeleteFlagEnum` 走约定式 `IPersistEnum` + `DeleteFlagEnumTypeHandler`
- **跨表事务**：共享 `TransactionTemplate`（`infrastructure/config/TransactionConfig`），禁 `@Transactional`
- **Redis 仅瞬态认证数据**：evict 写 `jti:*` 黑名单（TTL=token 剩余 exp）；不建业务缓存
- **权限码三段式**：模块/操作 `[a-z][a-z0-9-]+`（禁冒号），资源路径多级冒号分隔逐段非空，至少三段，按段校验，违规启动失败
- **管理面过滤**：仅 RBAC 角色/权限分页，硬编码 `create_user_id IS NULL OR create_user_id = userId`，SUPER_ADMIN（roleCodes 含 `SUPER_ADMIN`）绕过；**禁止复用业务表**（TODO 注释）
- **功能权限 vs 数据权限**：perms/roles=JWT 快照（滞后 ≤30min，变更踢人）；data_scope=实时 DB 查询（即时生效）；每个相关接口 javadoc 写明所属模型
- **设计模式三件套**：新增设计模式类必须附 角色说明 + 优缺点分析 + UML（Mermaid/ASCII）
- **构建**：`mvn clean package -DskipTests` 编译；`mvn test -Dtest=XxxTest` 跑测试；Maven 用本机 `D:\Tool\apache-maven-3.8.8\bin\mvn`（mvnw 不可用）
- **提交**：仅 `AI` 分支，conventional commit（feat/fix/docs 前缀）

---

### Task 1: UmsPermission 三件套（实体/PO/Mapper/Service）

**Files:**
- Create: `src/main/java/com/sanye/strategy/domain/rbac/entity/UmsPermission.java`
- Create: `src/main/java/com/sanye/strategy/domain/rbac/repository/UmsPermissionService.java`
- Create: `src/main/java/com/sanye/strategy/infrastructure/persistence/po/UmsPermissionPO.java`
- Create: `src/main/java/com/sanye/strategy/infrastructure/persistence/mapper/UmsPermissionMapper.java`
- Create: `src/main/java/com/sanye/strategy/infrastructure/persistence/impl/UmsPermissionServiceImpl.java`
- Create: `src/main/resources/mapper/UmsPermissionMapper.xml`
- Create: `src/main/java/com/sanye/strategy/domain/enums/PermissionTypeEnum.java`

**Interfaces:**
- Consumes: `BaseEntity`/`BasePO`/`IService`/`MpBaseServiceImpl`/`BeanCopyUtils`
- Produces: `UmsPermissionService extends IService<UmsPermission>` — Task 2/7/11/12 依赖；`PermissionTypeEnum`

权限资源表 `ums_permission`（`sql/rbac.sql`）列：parent_id/permission_name/permission_type/permission_code/route_path/component_path/api_method/api_path/icon/sort_order/is_frame/is_cache/is_visible/status/is_built_in/remark + BaseEntity 审计列。继承 BaseEntity（有人工管理需追溯操作人）。

- [ ] **Step 1: 建 PermissionTypeEnum**

```java
package com.sanye.strategy.domain.enums;

import com.baomidou.mybatisplus.annotation.EnumValue;
import lombok.Getter;

/** 权限资源类型 */
@Getter
public enum PermissionTypeEnum {
    DIRECTORY(1, "目录"), MENU(2, "菜单"), BUTTON(3, "按钮"), INTERFACE(4, "接口");
    @EnumValue
    private final int code;
    private final String desc;
    PermissionTypeEnum(int code, String desc) { this.code = code; this.desc = desc; }
    public static PermissionTypeEnum valueOf(int code) {
        for (PermissionTypeEnum e : values()) { if (e.code == code) { return e; } }
        return null;
    }
}
```

- [ ] **Step 2: 建实体 UmsPermission**

```java
package com.sanye.strategy.domain.rbac.entity;

import com.sanye.strategy.common.base.BaseEntity;
import com.sanye.strategy.domain.enums.PermissionTypeEnum;
import com.sanye.strategy.domain.enums.RoleStatusEnum;
import com.sanye.strategy.domain.enums.YesNoEnum;
import lombok.Data;
import lombok.EqualsAndHashCode;

/** 权限资源实体 — RBAC 目录/菜单/按钮/接口（纯 POJO） */
@Data
@EqualsAndHashCode(callSuper = true)
public class UmsPermission extends BaseEntity {
    private Long parentId;          // 父资源ID，0-根
    private String permissionName;  // 资源名称
    private PermissionTypeEnum permissionType;  // 类型：目录/菜单/按钮/接口
    private String permissionCode;  // 权限标识，如 system:user:create（按钮/接口用）
    private String routePath;       // 前端路由
    private String componentPath;   // 前端组件路径
    private String apiMethod;       // 接口请求方法 GET/POST/...
    private String apiPath;         // 接口路径
    private String icon;            // 图标
    private Integer sortOrder;      // 显示顺序
    private YesNoEnum isFrame;      // 是否外链
    private YesNoEnum isCache;      // 是否缓存
    private YesNoEnum isVisible;    // 是否显示
    private RoleStatusEnum status;  // 状态 0-停用 1-正常
    private YesNoEnum isBuiltIn;    // 是否内置资源
    private String remark;          // 备注
}
```

- [ ] **Step 3: 建 PO UmsPermissionPO**

复制实体字段 + `@TableName("ums_permission")` 继承 `BasePO`，包 `infrastructure/persistence/po`（模式同 `UmsRolePO`）。PO 字段与实体同名同类型（枚举共用），`BeanCopyUtils` 直接复制。

- [ ] **Step 4: 建 Mapper + XML**

```java
package com.sanye.strategy.infrastructure.persistence.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.sanye.strategy.infrastructure.persistence.po.UmsPermissionPO;

/** 权限资源 Mapper */
public interface UmsPermissionMapper extends BaseMapper<UmsPermissionPO> {
}
```

XML `src/main/resources/mapper/UmsPermissionMapper.xml`：namespace = 全限定名，含 `<resultMap>`（列→PO 属性，permission_type/status/is_built_in/is_frame/is_cache/is_visible 枚举映射）与 `<sql id="Base_Column_List">`。参照 `UmsRoleMapper.xml` 既有结构。

- [ ] **Step 5: 建 Service 契约 + 实现**

```java
package com.sanye.strategy.domain.rbac.repository;

import com.sanye.strategy.common.base.IService;
import com.sanye.strategy.domain.rbac.entity.UmsPermission;

/** 权限资源数据访问契约 */
public interface UmsPermissionService extends IService<UmsPermission> {
}
```

```java
package com.sanye.strategy.infrastructure.persistence.impl;

import com.sanye.strategy.common.base.MpBaseServiceImpl;
import com.sanye.strategy.common.util.BeanCopyUtils;
import com.sanye.strategy.domain.rbac.entity.UmsPermission;
import com.sanye.strategy.domain.rbac.repository.UmsPermissionService;
import com.sanye.strategy.infrastructure.persistence.mapper.UmsPermissionMapper;
import com.sanye.strategy.infrastructure.persistence.po.UmsPermissionPO;
import org.springframework.stereotype.Service;

@Service
public class UmsPermissionServiceImpl extends MpBaseServiceImpl<UmsPermissionPO, UmsPermissionMapper, UmsPermission>
        implements UmsPermissionService {
    @Override protected UmsPermissionPO toPO(UmsPermission e) { return BeanCopyUtils.copy(e, UmsPermissionPO.class); }
    @Override protected UmsPermission toEntity(UmsPermissionPO p) { return BeanCopyUtils.copy(p, UmsPermission.class); }
}
```

- [ ] **Step 6: 编译验证**

Run: `D:\Tool\apache-maven-3.8.8\bin\mvn clean compile -q`
Expected: BUILD SUCCESS

- [ ] **Step 7: Commit**

```bash
git add src/main/java/com/sanye/strategy/domain/rbac src/main/java/com/sanye/strategy/domain/enums/PermissionTypeEnum.java src/main/java/com/sanye/strategy/infrastructure/persistence src/main/resources/mapper/UmsPermissionMapper.xml
git commit -m "feat(rbac): UmsPermission 三件套 + PermissionTypeEnum"
```

---

### Task 2: UmsRolePermission 三件套 + 角色-权限契约

**Files:**
- Create: `src/main/java/com/sanye/strategy/domain/rbac/entity/UmsRolePermission.java`
- Create: `src/main/java/com/sanye/strategy/domain/rbac/repository/UmsRolePermissionService.java`
- Create: `src/main/java/com/sanye/strategy/infrastructure/persistence/po/UmsRolePermissionPO.java`
- Create: `src/main/java/com/sanye/strategy/infrastructure/persistence/mapper/UmsRolePermissionMapper.java`
- Create: `src/main/java/com/sanye/strategy/infrastructure/persistence/impl/UmsRolePermissionServiceImpl.java`
- Create: `src/main/resources/mapper/UmsRolePermissionMapper.xml`

**Interfaces:**
- Consumes: `UmsPermissionService`（Task 1）
- Produces: `UmsRolePermissionService`（自定义契约，物理删除表不继承 IService）— Task 5/7/11/12 依赖

`ums_role_permission` 物理删除关系表（无 deleted），列：role_id/permission_id/grant_user_id + create/update_time。

- [ ] **Step 1: 实体 + PO**

```java
package com.sanye.strategy.domain.rbac.entity;

import lombok.Data;

/** 角色-权限关联实体（物理删除关系表，无 deleted，不继承 base） */
@Data
public class UmsRolePermission {
    private Long id;         // 主键（雪花）
    private Long roleId;
    private Long permissionId;
    private Long grantUserId; // 授权人ID
    private java.time.LocalDateTime createTime;
    private java.time.LocalDateTime updateTime;
}
```

PO 同字段 + `@TableId(type = IdType.ASSIGN_ID)` + createTime/updateTime `@TableField(fill = INSERT)`（物理删除表无 deleted，`MetaObjectHandler.strictInsertFill` 按存在性填充）。参照 `UmsUserRolePO`。

- [ ] **Step 2: Mapper + XML**

```java
public interface UmsRolePermissionMapper extends BaseMapper<UmsRolePermissionPO> {
    /** 角色当前权限码列表（联表 ums_permission，过滤停用/逻辑删除） */
    List<String> selectPermissionCodesByRoleId(@Param("roleId") Long roleId);
    /** 批量授权（INSERT IGNORE，uk_role_permission 防重） */
    int insertIgnoreBatch(@Param("list") List<UmsRolePermissionPO> list);
}
```

XML：`selectPermissionCodesByRoleId` 联表：

```sql
SELECT p.permission_code
FROM ums_role_permission rp
INNER JOIN ums_permission p ON p.id = rp.permission_id
WHERE rp.role_id = #{roleId}
  AND p.deleted = 0
  AND p.status = 1
  AND p.permission_code IS NOT NULL AND p.permission_code != ''
ORDER BY p.sort_order ASC
```

```xml
<insert id="insertIgnoreBatch">
    INSERT IGNORE INTO ums_role_permission (id, role_id, permission_id, grant_user_id, create_time, update_time)
    VALUES
    <foreach collection="list" item="item" separator=",">
        (#{item.id}, #{item.roleId}, #{item.permissionId}, #{item.grantUserId}, NOW(), NOW())
    </foreach>
</insert>
<select id="selectPermissionIdsByRoleId" resultType="java.lang.Long">
    SELECT permission_id FROM ums_role_permission WHERE role_id = #{roleId}
</select>
<select id="selectRoleIdsByPermissionId" resultType="java.lang.Long">
    SELECT DISTINCT role_id FROM ums_role_permission WHERE permission_id = #{permissionId}
</select>
```

- [ ] **Step 3: Service 契约 + 实现**

```java
package com.sanye.strategy.domain.rbac.repository;

import java.util.List;

/** 角色-权限关联数据访问契约（物理删除表，直连 Mapper，不继承 IService） */
public interface UmsRolePermissionService {
    List<String> getPermissionCodesByRoleId(Long roleId);   // 角色当前生效权限码
    List<Long> getPermissionIdsByRoleId(Long roleId);       // 角色当前权限ID集（克隆/导入/覆盖对比）
    List<Long> getRoleIdsByPermissionId(Long permissionId); // 权限被哪些角色绑定（停用/删除前反查 evict）
    void grant(Long roleId, Long permissionId, Long grantUserId);            // 单条授权
    void grantBatch(Long roleId, List<Long> permissionIds, Long grantUserId); // 批量授权（INSERT IGNORE）
    void revoke(Long roleId, Long permissionId);            // 单条回收
    void revokeByRoleId(Long roleId);                       // 清空角色全部权限
    int countByPermissionId(Long permissionId);             // 权限被角色引用数（停用/删除前校验）
}
```

实现直连 `UmsRolePermissionMapper`（模式同 `UmsUserRoleServiceImpl`）。`grantBatch` 组装 PO 列表调 `insertIgnoreBatch`。`revoke` 用 BaseMapper `delete(Wrappers...)` 或 XML `delete where role_id and permission_id`。`revokeByRoleId` 用 BaseMapper delete by role_id。

- [ ] **Step 4: 编译验证 + Commit**

Run: `D:\Tool\apache-maven-3.8.8\bin\mvn clean compile -q` → BUILD SUCCESS

```bash
git add src/main/java/com/sanye/strategy/domain/rbac src/main/java/com/sanye/strategy/infrastructure/persistence src/main/resources/mapper/UmsRolePermissionMapper.xml
git commit -m "feat(rbac): UmsRolePermission 三件套 + 角色-权限契约（grant/revoke/查码）"
```

---

### Task 3: 扩展 UmsUserRoleService（覆盖绑定/批量授/解绑/续期/过期查询/角色在线用户）

**Files:**
- Create: `src/main/java/com/sanye/strategy/domain/user/entity/UmsUserRole.java`
- Modify: `src/main/java/com/sanye/strategy/domain/user/repository/UmsUserRoleService.java`
- Modify: `src/main/java/com/sanye/strategy/infrastructure/persistence/impl/UmsUserRoleServiceImpl.java`
- Modify: `src/main/java/com/sanye/strategy/infrastructure/persistence/mapper/UmsUserRoleMapper.java`
- Modify: `src/main/resources/mapper/UmsUserRoleMapper.xml`

**Interfaces:**
- Consumes: `UmsUserRoleService` 现有（getRoleCodesByUserId / assignRole）
- Produces: 扩展契约（Task 8/11/13 依赖）

- [ ] **Step 1: 实体 UmsUserRole**

```java
package com.sanye.strategy.domain.user.entity;

import lombok.Data;

/** 用户-角色关联实体（物理删除关系表，无 deleted，不继承 base） */
@Data
public class UmsUserRole {
    private Long id;
    private Long userId;
    private Long roleId;
    private java.time.LocalDateTime beginTime;
    private java.time.LocalDateTime endTime;
    private Long assignerId;
    private java.time.LocalDateTime createTime;
    private java.time.LocalDateTime updateTime;
}
```

> `UmsUserRolePO` 已存在（`infrastructure/persistence/po`），实体照其字段建。

- [ ] **Step 2: 契约扩展**

```java
/** 用户当前生效角色绑定行（时间窗内） */
List<UmsUserRole> listEffectiveByUserId(Long userId);
/** 覆盖绑定：清空用户现有角色 → 批量写入新角色集（begin/end 可为 null） */
void replaceRoles(Long userId, List<UmsUserRole> newRoles, Long assignerId);
/** 解绑用户某角色（物理删除） */
boolean removeUserRole(Long userId, Long roleId);
/** 续期：更新某绑定的 end_time，返回是否命中 */
boolean renew(Long userId, Long roleId, LocalDateTime endTime);
/** 已过期绑定（end_time 非 null 且 <= now），offset 分页（定时任务全量遍历防饿死） */
List<UmsUserRole> listExpired(LocalDateTime now, long offset, int limit);
/** 即将/已过期绑定（end_time 非 null 且 <= horizon），offset 分页（管理页 expiring 端点） */
List<UmsUserRole> listExpiring(LocalDateTime now, LocalDateTime horizon, long offset, int limit);
/** 角色下活动会话用户ID（联 ums_user_login_device is_current=1，供 evict），offset 分页 */
List<Long> listActiveUserIdsByRoleId(Long roleId, long offset, int limit);
/** 角色下用户数 */
long countUserIdsByRoleId(Long roleId);
/** 绑定行 ID 查绑定（批量续期反查 userId 用） */
UmsUserRole getById(Long bindId);
/** 按绑定行 ID 续期（批量续期接口用，返回是否命中） */
boolean renewById(Long bindId, LocalDateTime endTime);
```

- [ ] **Step 3: Mapper XML 方法**

`selectRoleCodesByUserId` 保留。新增：

```sql
<select id="selectEffectiveByUserId" resultType="com.sanye.strategy.infrastructure.persistence.po.UmsUserRolePO">
    SELECT ur.* FROM ums_user_role ur
    INNER JOIN ums_role r ON r.id = ur.role_id
    WHERE ur.user_id = #{userId} AND r.deleted = 0 AND r.status = 1
      AND (ur.begin_time IS NULL OR ur.begin_time &lt;= NOW())
      AND (ur.end_time IS NULL OR ur.end_time &gt; NOW())
</select>
<select id="selectExpired" resultType="com.sanye.strategy.infrastructure.persistence.po.UmsUserRolePO">
    SELECT * FROM ums_user_role
    WHERE end_time IS NOT NULL AND end_time &lt;= #{now}
    ORDER BY end_time ASC LIMIT #{limit} OFFSET #{offset}
</select>
<select id="selectExpiring" resultType="com.sanye.strategy.infrastructure.persistence.po.UmsUserRolePO">
    SELECT * FROM ums_user_role
    WHERE end_time IS NOT NULL AND end_time &lt;= #{horizon}
    ORDER BY end_time ASC LIMIT #{limit} OFFSET #{offset}
</select>
<select id="selectActiveUserIdsByRoleId" resultType="java.lang.Long">
    SELECT DISTINCT ur.user_id FROM ums_user_role ur
    INNER JOIN ums_user_login_device d ON d.user_id = ur.user_id AND d.is_current = 1 AND d.deleted = 0
    WHERE ur.role_id = #{roleId}
    LIMIT #{limit} OFFSET #{offset}
</select>
<select id="countUserIdsByRoleId" resultType="java.lang.Long">
    SELECT COUNT(DISTINCT user_id) FROM ums_user_role WHERE role_id = #{roleId}
</select>
<update id="updateEndTimeById">
    UPDATE ums_user_role SET end_time=#{endTime}, update_time=NOW() WHERE id=#{bindId}
</update>
```

覆盖绑定 `replaceRoles`：XML `deleteByUserId`（`DELETE FROM ums_user_role WHERE user_id=#{userId}`）→ 批量 insert（`insertIgnoreBatch` 模式同 Task2，或 BaseMapper insert 逐条）。`renew`：`UPDATE ums_user_role SET end_time=#{endTime}, update_time=NOW() WHERE user_id=#{userId} AND role_id=#{roleId}`。解绑：`DELETE WHERE user_id AND role_id`。**物理删，不引入 deleted。**

- [ ] **Step 4: 实现类扩展**

`UmsUserRoleServiceImpl` 直连 Mapper 实现全部新方法。`replaceRoles` 在调用方事务内（RbacManageService TransactionTemplate）执行 删+插。

- [ ] **Step 5: 编译验证 + Commit**

Run: `D:\Tool\apache-maven-3.8.8\bin\mvn clean compile -q` → BUILD SUCCESS

```bash
git add src/main/java/com/sanye/strategy/domain/user src/main/java/com/sanye/strategy/infrastructure/persistence src/main/resources/mapper/UmsUserRoleMapper.xml
git commit -m "feat(rbac): UmsUserRoleService 扩展 — 覆盖绑定/解绑/续期/过期查询/角色在线用户"
```

---

### Task 4: 鉴权注解 + PermissionCodeValidator + 单测

**Files:**
- Create: `src/main/java/com/sanye/strategy/infrastructure/security/RequiresPermission.java`
- Create: `src/main/java/com/sanye/strategy/infrastructure/security/NoPermissionRequired.java`
- Create: `src/main/java/com/sanye/strategy/infrastructure/security/PermissionCodeValidator.java`
- Create: `src/test/java/com/sanye/strategy/infrastructure/security/PermissionCodeValidatorTest.java`

**Interfaces:**
- Consumes: —
- Produces: `RequiresPermission(String value)` / `NoPermissionRequired`（Task 6/7/12 用）；`PermissionCodeValidator.validate(String)`（Task 7 扫描校验用）

**设计模式（校验器）：** 角色=权限码语法校验单例（纯函数）；优点：语法规则集中一处、按段校验防解析歧义、可单测；缺点：需扫描/拦截两调用点保证覆盖。

- [ ] **Step 1: 写失败单测**

```java
package com.sanye.strategy.infrastructure.security;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class PermissionCodeValidatorTest {
    @Test void acceptsStandardThreeSegment() {
        assertDoesNotThrow(() -> PermissionCodeValidator.validate("system:role:manage"));
    }
    @Test void acceptsMultiLevelResource() {
        assertDoesNotThrow(() -> PermissionCodeValidator.validate("system:rbac:debug:manage"));
    }
    @Test void acceptsNestedResourceModule() {
        // sys:tem:role:manage = 模块 sys + 资源 tem:role + 操作 manage，按段校验合法（资源可多级冒号分隔）
        assertDoesNotThrow(() -> PermissionCodeValidator.validate("sys:tem:role:manage"));
    }
    @Test void rejectsTwoSegments() {
        assertThrows(IllegalArgumentException.class, () -> PermissionCodeValidator.validate("system:manage"));
    }
    @Test void rejectsEmptySegment() {
        assertThrows(IllegalArgumentException.class, () -> PermissionCodeValidator.validate("system::manage"));
    }
    @Test void rejectsUppercaseOrSlash() {
        assertThrows(IllegalArgumentException.class, () -> PermissionCodeValidator.validate("System:role:manage"));
        assertThrows(IllegalArgumentException.class, () -> PermissionCodeValidator.validate("system/role/manage"));
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `D:\Tool\apache-maven-3.8.8\bin\mvn test -Dtest=PermissionCodeValidatorTest`
Expected: FAIL（编译错误，PermissionCodeValidator 不存在）

- [ ] **Step 3: 实现注解 + 校验器**

```java
package com.sanye.strategy.infrastructure.security;

import java.lang.annotation.*;

/** 接口鉴权注解 — value 为三段式权限码（模块:资源:操作），方法级优先类级兜底 */
@Retention(RetentionPolicy.RUNTIME)
@Target({ElementType.METHOD, ElementType.TYPE})
public @interface RequiresPermission {
    String value();
}

/** 显式豁免鉴权注解 — 标注类/方法跳过权限校验（仅需登录），不硬编码路径 */
@Retention(RetentionPolicy.RUNTIME)
@Target({ElementType.METHOD, ElementType.TYPE})
public @interface NoPermissionRequired {}
```

```java
package com.sanye.strategy.infrastructure.security;

import java.util.regex.Pattern;

/** 权限码语法校验（按段）— 模块/操作 `[a-z][a-z0-9-]+` 禁冒号，资源段可多级冒号分隔逐段非空 */
public final class PermissionCodeValidator {
    private static final Pattern SEGMENT = Pattern.compile("[a-z][a-z0-9-]+");

    private PermissionCodeValidator() {}

    /** 校验三段式权限码；不合规抛 IllegalArgumentException（扫描 fail-fast） */
    public static void validate(String code) {
        if (code == null || code.isBlank()) {
            throw new IllegalArgumentException("权限码不能为空");
        }
        String[] parts = code.split(":");
        if (parts.length < 3) {
            throw new IllegalArgumentException("权限码至少三段 模块:资源:操作: " + code);
        }
        if (!SEGMENT.matcher(parts[0]).matches()) {
            throw new IllegalArgumentException("权限码模块段非法(仅小写字母数字连字符，禁冒号): " + code);
        }
        for (int i = 1; i < parts.length - 1; i++) {
            if (!SEGMENT.matcher(parts[i]).matches()) {
                throw new IllegalArgumentException("权限码资源段非法: " + code);
            }
        }
        if (!SEGMENT.matcher(parts[parts.length - 1]).matches()) {
            throw new IllegalArgumentException("权限码操作段非法: " + code);
        }
    }
}
```

> 资源段多级（如 `rbac:debug`）自动落入逐段 `SEGMENT` 校验（每段独立非空 + 小写字母数字连字符）。

- [ ] **Step 4: 跑测试确认通过**

Run: `D:\Tool\apache-maven-3.8.8\bin\mvn test -Dtest=PermissionCodeValidatorTest`
Expected: PASS（6 tests）

- [ ] **Step 5: Commit**

```bash
git add src/main/java/com/sanye/strategy/infrastructure/security/RequiresPermission.java src/main/java/com/sanye/strategy/infrastructure/security/NoPermissionRequired.java src/main/java/com/sanye/strategy/infrastructure/security/PermissionCodeValidator.java src/test/java/com/sanye/strategy/infrastructure/security/PermissionCodeValidatorTest.java
git commit -m "feat(rbac): 鉴权注解 + PermissionCodeValidator 按段校验"
```

---

### Task 5: JWT perms claim（签发链路 + 体积上限）

**Files:**
- Modify: `src/main/java/com/sanye/strategy/infrastructure/security/JwtUtil.java`
- Modify: `src/main/java/com/sanye/strategy/application/auth/AuthService.java`
- Modify: `src/main/resources/application.yaml`

**Interfaces:**
- Consumes: `UmsRolePermissionService.getPermissionCodesByRoleId`（Task 2）、`UmsRoleService`/`UmsUserRoleService`
- Produces: `JwtUtil.generateAccessToken(userId, roleCodes, perms, jti, deviceId)`（五参新签名，全部调用点同步改）

- [ ] **Step 1: JwtUtil 加 perms claim + 体积上限**

```java
// JwtUtil 修改：类加 @Slf4j（Lombok），新增常量 + 配置 + 参数
private static final String CLAIM_PERMS = "perms";
private final long permClaimMax;

public JwtUtil(@Value("${jwt.secret}") String secret,
               @Value("${jwt.access-token-ttl-minutes:30}") long accessTokenTtlMinutes,
               @Value("${jwt.perm-claim-max:500}") long permClaimMax) {
    this.secretKey = Keys.hmacShaKeyFor(secret.getBytes(StandardCharsets.UTF_8));
    this.accessTokenTtlMillis = accessTokenTtlMinutes * 60_000L;
    this.permClaimMax = permClaimMax;
}

/** evict 用：accessToken TTL 秒数（会话无 expire_time 时兜底） */
public long getAccessTokenTtlSeconds() { return accessTokenTtlMillis / 1000L; }

public String generateAccessToken(Long userId, List<String> roleCodes, List<String> permCodes, Long jti, String deviceId) {
    // perms 超上限 → safe-degrade：置空 + ERROR 告警（宁拒勿越权）
    List<String> perms = permCodes == null ? List.of() : permCodes;
    if (perms.size() > permClaimMax) {
        log.error("用户 {} 权限码数量 {} 超上限 {}，perms claim 置空（safe-degrade）", userId, perms.size(), permClaimMax);
        perms = List.of();
    }
    Date now = new Date();
    return Jwts.builder()
            .header().keyId(KID).and()
            .claims(Map.of(
                    CLAIM_TYPE, TYPE_ACCESS,
                    CLAIM_USER_ID, userId,
                    CLAIM_ROLES, roleCodes == null ? List.of() : roleCodes,
                    CLAIM_PERMS, perms,
                    CLAIM_JTI, String.valueOf(jti),
                    CLAIM_DEVICE_ID, deviceId))
            .subject(String.valueOf(userId))
            .issuedAt(now)
            .expiration(new Date(now.getTime() + accessTokenTtlMillis))
            .signWith(secretKey, Jwts.SIG.HS256)
            .compact();
}
```

- [ ] **Step 2: AuthService 改签发调用 + loadPermCodes**

```java
// AuthService 注入 UmsRolePermissionService rolePermissionService;
// issueTokens(...) 与 refresh 中 generateAccessToken 调用改为五参：
String accessToken = jwtUtil.generateAccessToken(
        user.getId(), loadRoleCodes(user.getId()), loadPermCodes(user.getId()),
        session.getId(), session.getDeviceId());

/** 加载用户生效权限码并集（角色→权限码；SUPER_ADMIN 直通无需 perms，保持 JWT 小巧） */
private List<String> loadPermCodes(Long userId) {
    List<String> roleCodes = loadRoleCodes(userId);
    if (roleCodes.contains("SUPER_ADMIN")) { return List.of(); }
    Set<String> codes = new LinkedHashSet<>();
    for (String roleCode : roleCodes) {
        UmsRole role = roleService.getOne(new DefaultQueryWrapper<UmsRole>().eq("role_code", roleCode));
        if (role != null) {
            codes.addAll(rolePermissionService.getPermissionCodesByRoleId(role.getId()));
        }
    }
    return new ArrayList<>(codes);
}
```

`issueTokens`（AuthService:415）与 refresh 直签（AuthService:303）现有四参 `generateAccessToken(userId, roleCodes, jti, deviceId)` 改五参（perms 插在 roleCodes 之后）。

- [ ] **Step 3: application.yaml 加配置**

```yaml
jwt:
  secret: ${JWT_SECRET:dev-only-jwt-secret-0123456789abcdef0123456789abcdef}
  access-token-ttl-minutes: 30
  perm-claim-max: 500
```

- [ ] **Step 4: 搜调用点确认改齐 + 编译**

Run: `grep -rn "generateAccessToken" src/main/java` → 定义 1 处 + issueTokens 内 1 处 + refresh 直签 1 处，全改五参
Run: `D:\Tool\apache-maven-3.8.8\bin\mvn clean compile -q` → BUILD SUCCESS

- [ ] **Step 5: Commit**

```bash
git add src/main/java/com/sanye/strategy/infrastructure/security/JwtUtil.java src/main/java/com/sanye/strategy/application/auth/AuthService.java src/main/resources/application.yaml
git commit -m "feat(rbac): JWT perms claim 快照 + perm-claim-max 上限 safe-degrade"
```

---

### Task 6: PermissionInterceptor + UserContext.permCodes + 注册 + 单测

**Files:**
- Modify: `src/main/java/com/sanye/strategy/infrastructure/security/UserContext.java`
- Modify: `src/main/java/com/sanye/strategy/infrastructure/interceptor/TokenAuthInterceptor.java`
- Create: `src/main/java/com/sanye/strategy/infrastructure/interceptor/PermissionInterceptor.java`
- Modify: `src/main/java/com/sanye/strategy/infrastructure/config/WebMvcConfig.java`
- Create: `src/test/java/com/sanye/strategy/infrastructure/interceptor/PermissionInterceptorTest.java`

**Interfaces:**
- Consumes: `RequiresPermission`/`NoPermissionRequired`（Task 4）、`ResultCode.FORBIDDEN`（已存在）
- Produces: 拦截器（Task 12 全部 Controller 生效）

- [ ] **Step 1: UserContext 加 permCodes**

`UserContext` 构造器加 `List<String> permCodes` + getter。`TokenAuthInterceptor.preHandle` 解析 `perms` claim（模式同 `parseRolesClaim`，方法 `parsePermsClaim`）→ `new UserContext(userId, roleCodes, permCodes, jti, deviceId)`。

- [ ] **Step 2: 写失败单测**

```java
package com.sanye.strategy.infrastructure.interceptor;

import com.sanye.strategy.common.exception.BizException;
import com.sanye.strategy.infrastructure.security.NoPermissionRequired;
import com.sanye.strategy.infrastructure.security.RequiresPermission;
import com.sanye.strategy.infrastructure.security.UserContext;
import jakarta.servlet.http.HttpServletRequest;
import org.junit.jupiter.api.*;
import org.springframework.web.method.HandlerMethod;

import java.util.List;

import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.Mockito.*;

class PermissionInterceptorTest {
    private PermissionInterceptor interceptor;

    @BeforeEach void setUp() {
        interceptor = new PermissionInterceptor();
        UserContext.clear();
    }
    @AfterEach void tearDown() { UserContext.clear(); }

    private UserContext ctx(List<String> roles, List<String> perms) {
        return new UserContext(1L, roles, perms, 111L, "dev1");
    }

    @Test void superAdminBypasses() {
        UserContext.set(ctx(List.of("SUPER_ADMIN"), List.of()));
        assertTrue(interceptor.preHandle(mockReq("/rbac/roles"), null, handlerWith("system:role:manage")));
    }
    @Test void permitsWhenClaimContainsCode() {
        UserContext.set(ctx(List.of("OPERATOR"), List.of("system:role:manage")));
        assertTrue(interceptor.preHandle(mockReq("/rbac/roles"), null, handlerWith("system:role:manage")));
    }
    @Test void forbidsWhenClaimMissingCode() {
        UserContext.set(ctx(List.of("OPERATOR"), List.of("system:permission:manage")));
        assertThrows(BizException.class,
                () -> interceptor.preHandle(mockReq("/rbac/roles"), null, handlerWith("system:role:manage")));
    }
    @Test void permitsWhenNoAnnotationOnNonRbacPath() {
        UserContext.set(ctx(List.of("OPERATOR"), List.of()));
        assertTrue(interceptor.preHandle(mockReq("/auth/logout"), null, handlerWithout()));
    }
    @Test void rbacPathWithoutAnnotationIsForbidden() {
        UserContext.set(ctx(List.of("OPERATOR"), List.of()));
        assertThrows(BizException.class,
                () -> interceptor.preHandle(mockReq("/rbac/roles"), null, handlerWithout()));
    }
    @Test void noPermissionRequiredSkipsEvenOnRbacPath() {
        UserContext.set(ctx(List.of("OPERATOR"), List.of()));
        assertTrue(interceptor.preHandle(mockReq("/rbac/my/permissions"), null, handlerNoPerm()));
    }

    private HttpServletRequest mockReq(String uri) {
        HttpServletRequest req = mock(HttpServletRequest.class);
        when(req.getRequestURI()).thenReturn(uri);
        return req;
    }
    private HandlerMethod handlerWith(String code) {
        RequiresPermission a = mock(RequiresPermission.class);
        when(a.value()).thenReturn(code);
        HandlerMethod h = mock(HandlerMethod.class);
        when(h.getMethodAnnotation(RequiresPermission.class)).thenReturn(a);
        return h;
    }
    private HandlerMethod handlerWithout() {
        HandlerMethod h = mock(HandlerMethod.class);
        when(h.getMethodAnnotation(RequiresPermission.class)).thenReturn(null);
        when(h.getMethodAnnotation(NoPermissionRequired.class)).thenReturn(null);
        when(h.getBeanType()).thenReturn(PlainController.class);
        return h;
    }
    private HandlerMethod handlerNoPerm() {
        HandlerMethod h = mock(HandlerMethod.class);
        when(h.getMethodAnnotation(RequiresPermission.class)).thenReturn(null);
        when(h.getMethodAnnotation(NoPermissionRequired.class)).thenReturn(mock(NoPermissionRequired.class));
        return h;
    }
    static class PlainController {}   // 无任何注解：/rbac 无注解 → 403；非 /rbac → 放行
    @NoPermissionRequired static class WithoutController {}   // 类级豁免（另一用例）
}
```

- [ ] **Step 3: 实现 PermissionInterceptor**

```java
package com.sanye.strategy.infrastructure.interceptor;

import com.sanye.strategy.common.exception.BizException;
import com.sanye.strategy.common.response.ResultCode;
import com.sanye.strategy.infrastructure.security.NoPermissionRequired;
import com.sanye.strategy.infrastructure.security.RequiresPermission;
import com.sanye.strategy.infrastructure.security.UserContext;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;
import org.springframework.stereotype.Component;
import org.springframework.web.method.HandlerMethod;
import org.springframework.web.servlet.HandlerInterceptor;

/**
 * 权限拦截器 — 责任链一环（认证之后）
 * <pre>
 * HandlerMethod? → @NoPermissionRequired? → @RequiresPermission? → SUPER_ADMIN? → perms claim 比对 → 放行/403
 * /rbac/** 无注解默认 403（防管理接口漏标注解）
 * </pre>
 * 零 DB 查询（perms 为 JWT 快照）。角色：Handler（认证后鉴权节点）；优点：接口级鉴权收口一处、无 DB 压力；
 * 缺点：perms 快照最长 30min 滞后，变更需踢人（RbacManageService 自动 evict 兜底）。
 */
@Component
public class PermissionInterceptor implements HandlerInterceptor {

    private static final String RBAC_PATH_PREFIX = "/rbac/";
    private static final String SUPER_ADMIN = "SUPER_ADMIN";

    @Override
    public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler) {
        if (!(handler instanceof HandlerMethod method)) { return true; }
        if (method.getMethodAnnotation(NoPermissionRequired.class) != null
                || method.getBeanType().getAnnotation(NoPermissionRequired.class) != null) { return true; }

        RequiresPermission requires = method.getMethodAnnotation(RequiresPermission.class);
        if (requires == null) { requires = method.getBeanType().getAnnotation(RequiresPermission.class); }

        if (requires == null) {
            String uri = request.getRequestURI();
            if (uri != null && uri.startsWith(RBAC_PATH_PREFIX)) {
                throw new BizException(ResultCode.FORBIDDEN, "接口未配置权限点");
            }
            return true;
        }
        UserContext ctx = UserContext.get();
        if (ctx == null) { throw new BizException(ResultCode.UNAUTHORIZED); }
        if (ctx.getRoleCodes().contains(SUPER_ADMIN)) { return true; }
        if (ctx.getPermCodes().contains(requires.value())) { return true; }
        throw new BizException(ResultCode.FORBIDDEN);
    }
}
```

- [ ] **Step 4: WebMvcConfig 注册（认证之后）**

```java
private final PermissionInterceptor permissionInterceptor;
registry.addInterceptor(tokenAuthInterceptor).addPathPatterns("/**").excludePathPatterns(WHITE_LIST);
registry.addInterceptor(permissionInterceptor).addPathPatterns("/**").excludePathPatterns(WHITE_LIST);
```

**顺序：** TokenAuthInterceptor 先注册、PermissionInterceptor 后注册（MVC 按注册序执行，认证先于鉴权）。

- [ ] **Step 5: 跑测试通过 + 编译**

Run: `D:\Tool\apache-maven-3.8.8\bin\mvn test -Dtest=PermissionInterceptorTest` → PASS
Run: `D:\Tool\apache-maven-3.8.8\bin\mvn clean compile -q` → BUILD SUCCESS

- [ ] **Step 6: Commit**

```bash
git add src/main/java/com/sanye/strategy/infrastructure/interceptor src/main/java/com/sanye/strategy/infrastructure/config/WebMvcConfig.java src/main/java/com/sanye/strategy/infrastructure/security/UserContext.java src/test/java/com/sanye/strategy/infrastructure/interceptor/PermissionInterceptorTest.java
git commit -m "feat(rbac): PermissionInterceptor 接口鉴权 + UserContext.permCodes + /rbac 无注解收紧"
```

---

### Task 7: 权限扫描注册（启动新增/复活 + 手动同步）

**Files:**
- Create: `src/main/java/com/sanye/strategy/infrastructure/config/PermissionScanRegistrar.java`
- Create: `src/main/java/com/sanye/strategy/application/rbac/PermissionSyncService.java`
- Create: `src/main/java/com/sanye/strategy/application/rbac/SyncReport.java`
- Create: `src/test/java/com/sanye/strategy/application/rbac/PermissionSyncServiceTest.java`

**Interfaces:**
- Consumes: `UmsPermissionService`（Task 1）、`PermissionCodeValidator`（Task 4）、`RequiresPermission`（Task 4）
- Produces: `PermissionSyncService.sync(boolean dryRun): SyncReport` — Task 11/12 用；启动 `ApplicationRunner` 自动执行

**同步语义（防竞态）：**
- 新增：`INSERT IGNORE`（uk_permission_code）
- 复活：`UPDATE SET status=1 WHERE status=0`（幂等）
- 残留停用：**仅手动同步执行**（启动不做）；`UPDATE SET status=0 WHERE status=1`（幂等）
- 并发：同实例 ReentrantLock 串行（集群 TODO）

- [ ] **Step 1: 单测（mock UmsPermissionService）**

```java
// sync(false)：扫描 3 注解码 → 新增 2、复活 1、残留 1 停用，断言 SyncReport 计数 + deprecatedCodes
// sync(true)：dryRun 返回差异，verify 无 insert/update 写调用
// 权限码违规 → 抛 IllegalArgumentException（fail-fast）
```

- [ ] **Step 2: 实现 PermissionSyncService**

```java
package com.sanye.strategy.application.rbac;

/** 权限资源扫描同步 — 启动新增/复活 + 手动残留停用（dry-run 预览）
 * <p>设计：启动只做新增+复活，残留停用由手动 sync 执行（防误停用）；状态变更幂等（WHERE status != target）。</p> */
@Service
@RequiredArgsConstructor
@Slf4j
public class PermissionSyncService {

    private static final String BASE_PACKAGE = "com.sanye.strategy.interfaces";
    private final UmsPermissionService permissionService;
    private final ReentrantLock lock = new ReentrantLock();

    public SyncReport sync(boolean dryRun) { return doSync(dryRun, true); }

    /** 启动同步：只做新增+复活，不做残留停用（防误停用，发布流程强制手动 sync） */
    public SyncReport syncStartup() { return doSync(false, false); }

    private SyncReport doSync(boolean dryRun, boolean includeDeprecated) {
        lock.lock();
        try {
            Map<String, String> scanned = scanAnnotatedCodes();
            List<UmsPermission> existing = permissionService.list(new DefaultQueryWrapper<UmsPermission>());
            Map<String, UmsPermission> byCode = existing.stream()
                    .filter(p -> p.getPermissionCode() != null)
                    .collect(Collectors.toMap(UmsPermission::getPermissionCode, p -> p, (a, b) -> a));
            SyncReport report = new SyncReport();
            for (Map.Entry<String, String> e : scanned.entrySet()) {
                UmsPermission p = byCode.get(e.getKey());
                if (p == null) {
                    report.getAdded().add(e.getKey());
                    if (!dryRun) { insertInterfacePermission(e.getKey(), e.getValue()); }
                } else if (RoleStatusEnum.DISABLED.equals(p.getStatus())) {
                    report.getRevived().add(e.getKey());
                    if (!dryRun) {
                        p.setStatus(RoleStatusEnum.NORMAL);
                        permissionService.updateById(p);
                    }
                }
            }
            for (UmsPermission p : existing) {
                if (includeDeprecated
                        && p.getPermissionCode() != null && PermissionTypeEnum.INTERFACE.equals(p.getPermissionType())
                        && RoleStatusEnum.NORMAL.equals(p.getStatus())
                        && !scanned.containsKey(p.getPermissionCode())) {
                    report.getDeprecated().add(p.getPermissionCode());
                    if (!dryRun) {
                        p.setStatus(RoleStatusEnum.DISABLED);
                        permissionService.updateById(p);   // 幂等：重复执行不重复停用
                    }
                }
            }
            return report;
        } finally { lock.unlock(); }
    }

    /** 扫 interfaces 包类级+方法级 @RequiresPermission，PermissionCodeValidator 校验（fail-fast） */
    private Map<String, String> scanAnnotatedCodes() {
        Map<String, String> result = new LinkedHashMap<>();
        ClassPathScanningCandidateComponentProvider scanner = new ClassPathScanningCandidateComponentProvider(false);
        scanner.addIncludeFilter(new AnnotationTypeFilter(RequiresPermission.class));
        // 类级无 @RequiresPermission 但方法级有 → 靠 @RestController 命中（AnnotationTypeFilter 匹配元注解，@RestController 含 @Controller）
        // AssignableTypeFilter(Controller.class) 是错误用法（注解接口不会 isAssignableFrom 具体控制器），已移除
        scanner.addIncludeFilter(new AnnotationTypeFilter(org.springframework.web.bind.annotation.RestController.class));
        for (BeanDefinition bd : scanner.findCandidateComponents(BASE_PACKAGE)) {
            String className = bd.getBeanClassName();
            try {
                Class<?> clazz = Class.forName(className);
                RequiresPermission classAnn = clazz.getAnnotation(RequiresPermission.class);
                if (classAnn != null) {
                    PermissionCodeValidator.validate(classAnn.value());
                    result.put(classAnn.value(), clazz.getSimpleName());
                }
                for (Method m : clazz.getMethods()) {
                    RequiresPermission methodAnn = m.getAnnotation(RequiresPermission.class);
                    if (methodAnn != null) {
                        PermissionCodeValidator.validate(methodAnn.value());
                        result.put(methodAnn.value(), clazz.getSimpleName() + "#" + m.getName());
                    }
                }
            } catch (ClassNotFoundException e) {
                throw new IllegalStateException("RBAC 权限扫描类加载失败: " + className, e);
            }
        }
        return result;
    }

    private void insertInterfacePermission(String code, String source) {
        UmsPermission p = new UmsPermission();
        p.setParentId(0L);
        p.setPermissionName(code);
        p.setPermissionType(PermissionTypeEnum.INTERFACE);
        p.setPermissionCode(code);
        p.setStatus(RoleStatusEnum.NORMAL);
        p.setIsBuiltIn(YesNoEnum.NO);
        p.setSortOrder(0);
        p.setRemark("注解自动注册: " + source);
        try {
            permissionService.insert(p);   // 主键回填 + uk_permission_code
        } catch (DuplicateKeyException e) {
            log.warn("权限码 {} 已存在（并发扫描/重复注册），跳过", code);   // 等价 INSERT IGNORE
        }
    }
}
```

> `insertInterfacePermission` 冲突处理：uk_permission_code 并发/重复 → 捕获 `DuplicateKeyException` 忽略并 `log.warn`（等价 INSERT IGNORE）。**实现时在 permissionService.insert 外层 try/catch DuplicateKeyException。**

- [ ] **Step 3: PermissionScanRegistrar（启动 runner）**

```java
/** 启动权限扫描注册 — 只做新增+复活，不做残留停用（防误停用，发布流程强制手动 sync） */
@Component
@RequiredArgsConstructor
@Slf4j
public class PermissionScanRegistrar implements ApplicationRunner {
    private final PermissionSyncService syncService;
    @Value("${rbac.sync-on-startup:true}") private boolean syncOnStartup;
    @Override public void run(ApplicationArguments args) {
        if (syncOnStartup) {
            SyncReport r = syncService.syncStartup();
            log.info("RBAC 权限启动同步: 新增{}, 复活{}, 残留停用{}", r.getAdded().size(), r.getRevived().size(), r.getDeprecated().size());
        }
    }
}
```

- [ ] **Step 4: SyncReport VO**

```java
/** 权限同步差异报告 */
@Data
public class SyncReport {
    private List<String> added = new ArrayList<>();
    private List<String> revived = new ArrayList<>();
    private List<String> deprecated = new ArrayList<>();
    private List<String> ignored = new ArrayList<>();   // 导入忽略的未注册权限码（告警用）
}
```

- [ ] **Step 5: 跑测试 + 编译 + Commit**

Run: `D:\Tool\apache-maven-3.8.8\bin\mvn test -Dtest=PermissionSyncServiceTest` → PASS
Run: `D:\Tool\apache-maven-3.8.8\bin\mvn clean compile -q` → BUILD SUCCESS

```bash
git add src/main/java/com/sanye/strategy/infrastructure/config/PermissionScanRegistrar.java src/main/java/com/sanye/strategy/application/rbac src/test/java/com/sanye/strategy/application/rbac
git commit -m "feat(rbac): 权限扫描注册（启动新增/复活 + 手动同步残留停用 + dry-run）"
```

---

### Task 8: EvictService（jti 黑名单踢人）+ 异步任务注册表

**Files:**
- Modify: `src/main/java/com/sanye/strategy/domain/user/repository/UmsUserLoginDeviceService.java`
- Modify: `src/main/java/com/sanye/strategy/infrastructure/persistence/impl/UmsUserLoginDeviceServiceImpl.java`
- Modify: `src/main/java/com/sanye/strategy/infrastructure/persistence/mapper/UmsUserLoginDeviceMapper.java`
- Modify: `src/main/resources/mapper/UmsUserLoginDeviceMapper.xml`
- Create: `src/main/java/com/sanye/strategy/application/rbac/EvictService.java`
- Create: `src/main/java/com/sanye/strategy/application/rbac/EvictTaskRegistry.java`
- Create: `src/main/java/com/sanye/strategy/interfaces/rbac/vo/EvictTaskVO.java`
- Create: `src/test/java/com/sanye/strategy/application/rbac/EvictServiceTest.java`

**Interfaces:**
- Consumes: `JtiBlacklistService.revoke(jti, ttl)`、`UmsUserLoginDeviceService`（会话查询）、`UmsUserRoleService`（Task 3 角色在线用户）、`JwtUtil.getAccessTokenTtlSeconds()`
- Produces: `EvictService.evictUsers(Collection<Long>)` / `evictRoleUsers(roleId)`；`EvictTaskRegistry.submit/get` — Task 11/12/13 依赖

- [ ] **Step 1: 会话查询契约**

`UmsUserLoginDeviceService` 加：`List<UmsUserLoginDevice> listActiveSessionsByUserIds(Collection<Long> userIds)`。Mapper XML：

```sql
<select id="selectActiveSessionsByUserIds" resultType="com.sanye.strategy.infrastructure.persistence.po.UmsUserLoginDevicePO">
    SELECT * FROM ums_user_login_device
    WHERE user_id IN (<foreach collection="userIds" item="id" separator=",">#{id}</foreach>)
      AND is_current = 1 AND deleted = 0
</select>
```

`UmsUserLoginDevice` 实体有 `expireTime`（`ums_user_login_device.expire_time` Token 过期时间）。

- [ ] **Step 2: 单测（mock）**

```java
// evictUsers：mock deviceService 返 2 会话（expireTime 未过期 + 已过期）
// 断言仅未过期 revoke 被调 1 次、TTL=剩余秒（向上取整 ≥1）
// 空 user 集合 → 0 调用
```

- [ ] **Step 3: 实现 EvictService**

```java
package com.sanye.strategy.application.rbac;

/** 用户踢下线服务 — 查活动会话写 jti 黑名单（TTL=token 剩余 exp）
 * <p>功能权限 JWT 快照变更后经此立即收回 accessToken（jti 黑名单秒级冻结）。
 * 与数据权限（实时）无关；本服务只收权限快照。</p> */
@Service
@RequiredArgsConstructor
@Slf4j
public class EvictService {
    private final UmsUserLoginDeviceService deviceService;
    private final UmsUserRoleService userRoleService;
    private final JtiBlacklistService jtiBlacklistService;
    private final JwtUtil jwtUtil;
    private static final int EVICT_RETRY = 3;
    private static final long BACKOFF_BASE_MS = 100L;

    /** 踢用户集：查活动会话，写 jti 黑名单（TTL=token 剩余），已过期会话跳过。返回踢中会话数 */
    public int evictUsers(Collection<Long> userIds) {
        if (userIds == null || userIds.isEmpty()) { return 0; }
        List<UmsUserLoginDevice> sessions = deviceService.listActiveSessionsByUserIds(userIds);
        int kicked = 0;
        for (UmsUserLoginDevice s : sessions) {
            long ttl = remainingTtlSeconds(s);
            if (ttl <= 0) { continue; }
            revokeWithRetry(s.getId(), ttl);
            kicked++;
        }
        return kicked;
    }

    /** 角色下活动用户 evict（同步，offset 分页遍历，供自动链路小量 + 调试接口） */
    public int evictRoleUsers(Long roleId) {
        int kicked = 0;
        long offset = 0;
        List<Long> ids;
        do {
            ids = userRoleService.listActiveUserIdsByRoleId(roleId, offset, 500);
            offset += ids.size();
            kicked += evictUsers(ids);
        } while (ids.size() == 500);   // 无 offset 会反复返回同一批 500，用户 501+ 永不踢
        return kicked;
    }

    /** Redis 抖动重试 3 次（指数退避）；仍失败上抛 → 调用方审计失败状态（权限变更已生效，仅踢人延迟） */
    private void revokeWithRetry(Long jti, long ttl) {
        for (int i = 0; i < EVICT_RETRY; i++) {
            try { jtiBlacklistService.revoke(jti, ttl); return; }
            catch (Exception e) {
                if (i == EVICT_RETRY - 1) { log.error("evict jti={} 重试{}次仍失败", jti, EVICT_RETRY, e); throw e; }
                try { Thread.sleep(BACKOFF_BASE_MS << i); }
                catch (InterruptedException ie) { Thread.currentThread().interrupt(); throw e; }
            }
        }
    }

    private long remainingTtlSeconds(UmsUserLoginDevice session) {
        if (session.getExpireTime() == null) { return jwtUtil.getAccessTokenTtlSeconds(); }
        long seconds = Duration.between(LocalDateTime.now(), session.getExpireTime()).getSeconds();
        return Math.max(1, seconds);
    }
}
```

> `EvictService` 需注入 `UmsUserRoleService`（evictRoleUsers 用）。`@RequiredArgsConstructor` + final 字段。

- [ ] **Step 4: EvictTaskRegistry + EvictTaskVO（异步，独立线程池）**

```java
/** 异步批量踢任务注册表 — 内存 ConcurrentHashMap + 独立线程池
 * <p>受限：服务重启丢任务（生产大批量变更需规避，见 spec 待办）；任务完成后清理防内存泄漏。</p> */
@Service
public class EvictTaskRegistry {
    private static final int MAX_RETAIN = 100;
    private static final Duration RETAIN_DURATION = Duration.ofHours(24);
    private final ConcurrentHashMap<String, EvictTaskVO> tasks = new ConcurrentHashMap<>();
    private final ExecutorService executor = Executors.newFixedThreadPool(
            Math.max(2, Runtime.getRuntime().availableProcessors() / 2),
            r -> { Thread t = new Thread(r, "evict-worker"); t.setDaemon(true); return t; });

    public String submit(Supplier<Integer> action, String sourceDesc) {
        String taskId = "evict_" + UUID.randomUUID().toString().replace("-", "").substring(0, 12);
        EvictTaskVO vo = new EvictTaskVO();
        vo.setTaskId(taskId);
        vo.setSourceDesc(sourceDesc);
        vo.setStatus("PENDING");
        vo.setCreatedAt(LocalDateTime.now());
        tasks.put(taskId, vo);
        executor.submit(() -> {
            vo.setStatus("RUNNING");
            try { vo.setKicked(action.get()); vo.setStatus("SUCCESS"); }
            catch (Exception e) { vo.setStatus("FAILED"); vo.setError(e.getMessage()); }
            finally { vo.setDoneAt(LocalDateTime.now()); }
        });
        evictStale();
        return taskId;
    }

    public EvictTaskVO get(String taskId) { return tasks.get(taskId); }

    private void evictStale() {
        List<String> stale = tasks.entrySet().stream()
                .filter(e -> e.getValue().getDoneAt() != null
                        && e.getValue().getDoneAt().plus(RETAIN_DURATION).isBefore(LocalDateTime.now()))
                .map(Map.Entry::getKey).toList();
        stale.forEach(tasks::remove);
        int overflow = tasks.size() - MAX_RETAIN;
        if (overflow > 0) { tasks.keySet().stream().sorted().limit(overflow).forEach(tasks::remove); }
    }
}
```

`EvictTaskVO`（`interfaces/rbac/vo`）：`{taskId, sourceDesc, status, kicked, error, createdAt, doneAt}`（Lombok @Data）。

- [ ] **Step 5: 跑测试 + 编译 + Commit**

Run: `D:\Tool\apache-maven-3.8.8\bin\mvn test -Dtest=EvictServiceTest` → PASS
Run: `D:\Tool\apache-maven-3.8.8\bin\mvn clean compile -q` → BUILD SUCCESS

```bash
git add src/main/java/com/sanye/strategy/application/rbac src/main/java/com/sanye/strategy/interfaces/rbac src/main/java/com/sanye/strategy/domain/user/repository src/main/java/com/sanye/strategy/infrastructure/persistence src/main/resources/mapper src/test/java/com/sanye/strategy/application/rbac
git commit -m "feat(rbac): EvictService 踢人（TTL 取剩余）+ 异步任务注册表 + 会话查询契约"
```

---

### Task 9: 审计基础设施（ums_oper_log + OperLogService REQUIRES_NEW）

**Files:**
- Create: `src/main/java/com/sanye/strategy/infrastructure/persistence/po/UmsOperLogPO.java`
- Create: `src/main/java/com/sanye/strategy/infrastructure/persistence/mapper/UmsOperLogMapper.java`
- Create: `src/main/resources/mapper/UmsOperLogMapper.xml`
- Create: `src/main/java/com/sanye/strategy/domain/enums/OperTypeEnum.java`
- Create: `src/main/java/com/sanye/strategy/application/rbac/OperLogService.java`
- Create: `src/main/java/com/sanye/strategy/application/rbac/OperLogReq.java`

**Interfaces:**
- Consumes: `UserContext`、`PlatformTransactionManager`（TransactionConfig 共享 Bean）、`IpUtils`
- Produces: `OperLogService.record(OperLogReq)`（REQUIRES_NEW 独立事务）— Task 11/12/13 用

- [ ] **Step 1: OperTypeEnum（含 EVICT_USER）**

```java
package com.sanye.strategy.domain.enums;

import lombok.Getter;

/** 操作日志类型 */
@Getter
public enum OperTypeEnum {
    CREATE(1, "新增"), UPDATE(2, "修改"), DELETE(3, "删除"), QUERY(4, "查询"),
    GRANT(5, "授权"), IMPORT(6, "导入"), EXPORT(7, "导出"), OTHER(8, "其他"),
    EVICT_USER(9, "踢下线");
    private final int code;
    private final String desc;
    OperTypeEnum(int code, String desc) { this.code = code; this.desc = desc; }
    public static OperTypeEnum valueOf(int code) {
        for (OperTypeEnum e : values()) { if (e.code == code) { return e; } }
        return null;
    }
}
```

- [ ] **Step 2: UmsOperLogPO + Mapper + XML**

`ums_oper_log` 列（oper_log.sql）：user_id/username/oper_module/oper_action/oper_desc/oper_type/request_method/request_uri/request_params/request_body/response_code/response_msg/cost_time/oper_ip/user_agent/status/error_msg/oper_time/create_time。**不继承 SimpleBasePO**（无 deleted/update_time），PO 手工 `@TableId(type = IdType.ASSIGN_ID)` + create_time `@TableField(fill = INSERT)` + `@TableName("ums_oper_log")`。Mapper `extends BaseMapper<UmsOperLogPO>`，XML 只 `<resultMap>` + `Base_Column_List`。

- [ ] **Step 3: OperLogService（独立事务）**

```java
/** 操作日志服务 — REQUIRES_NEW 独立事务：业务回滚也留痕；写审计失败不影响主流程 */
@Service
@RequiredArgsConstructor
@Slf4j
public class OperLogService {
    private final PlatformTransactionManager txManager;
    private final UmsOperLogMapper operLogMapper;

    public void record(OperLogReq req) {
        try {
            TransactionTemplate tpl = new TransactionTemplate(txManager);
            tpl.setPropagationBehavior(TransactionDefinition.PROPAGATION_REQUIRES_NEW);
            tpl.executeWithoutResult(status -> {
                UserContext ctx = UserContext.get();
                UmsOperLogPO po = new UmsOperLogPO();
                po.setUserId(ctx == null ? null : ctx.getUserId());
                po.setUsername(ctx == null ? null : String.valueOf(ctx.getUserId()));
                po.setOperModule(req.getModule());
                po.setOperAction(req.getAction());
                po.setOperDesc(req.getDesc());
                po.setOperType(req.getType() == null ? OperTypeEnum.OTHER.getCode() : req.getType().getCode());
                po.setRequestMethod(req.getRequestMethod());
                po.setRequestUri(req.getRequestUri());
                po.setOperIp(IpUtils.getClientIp());
                po.setUserAgent(req.getUserAgent());
                po.setStatus(req.isSuccess() ? 1 : 0);
                po.setErrorMsg(req.getErrorMsg());
                po.setOperTime(LocalDateTime.now());
                operLogMapper.insert(po);
            });
        } catch (Exception e) {
            log.error("审计日志写入失败，降级不影响主流程", e);
        }
    }
}
```

`OperLogReq`：`{module, action, desc, type, requestMethod, requestUri, userAgent, success, errorMsg}`（@Data + builder 可选）。

- [ ] **Step 4: 编译 + Commit**

Run: `D:\Tool\apache-maven-3.8.8\bin\mvn clean compile -q` → BUILD SUCCESS

```bash
git add src/main/java/com/sanye/strategy/infrastructure/persistence src/main/resources/mapper/UmsOperLogMapper.xml src/main/java/com/sanye/strategy/application/rbac/OperLogService.java src/main/java/com/sanye/strategy/application/rbac/OperLogReq.java src/main/java/com/sanye/strategy/domain/enums/OperTypeEnum.java
git commit -m "feat(rbac): 审计基础设施 — ums_oper_log + OperLogService(REQUIRES_NEW) + OperTypeEnum EVICT_USER"
```

---

### Task 10: DataScopeBuilder（数据权限 1/2 级）

**Files:**
- Create: `src/main/java/com/sanye/strategy/infrastructure/security/DataScopeBuilder.java`
- Create: `src/test/java/com/sanye/strategy/infrastructure/security/DataScopeBuilderTest.java`

**Interfaces:**
- Consumes: `UserContext`、`UmsRoleService`（查 data_scope）、`DataScopeEnum`（已存在）
- Produces: `DataScopeBuilder.applyDataScope(IWrapper<T>, String ownerColumn)` — 业务查询方调用（本期业务表不落地，仅基础设施 + 单测）

- [ ] **Step 1: 写失败单测**

```java
// data_scope=1（任一角色）→ 无 eq 条件
// data_scope=2（全部角色）→ 追加 eq(ownerColumn, ctx.userId)
// 空角色（有 ctx，userId 非 null）→ eq(ownerColumn, userId)（仅本人安全默认）
// 无上下文（ctx null，无 userId 可滤）→ 返回原 wrapper 不加条件（仅启动/后台场景，认证链路必有 ctx）
// 混合（一角色=1 一角色=2）→ 无条件（data_scope=1 优先生效）
// 含 SUPER_ADMIN → 无条件
```

- [ ] **Step 2: 实现**

```java
package com.sanye.strategy.infrastructure.security;

/** 数据权限装配（方案 C，本期 1/2 级）— 实时 DB 查角色 data_scope，即时生效
 * <p>与 RBAC 功能权限（JWT perms 快照）不同：本组件每次请求实时查询，data_scope 变更下请求即生效。
 * 管理面 create_user_id 过滤（谁创建看谁）与本组件是两套机制，禁止复用业务表。</p> */
@Component
@RequiredArgsConstructor
public class DataScopeBuilder {
    private static final String SUPER_ADMIN = "SUPER_ADMIN";
    private final UmsRoleService roleService;

    public <T> IWrapper<T> applyDataScope(IWrapper<T> wrapper, String ownerColumn) {
        UserContext ctx = UserContext.get();
        List<String> roleCodes = ctx == null ? List.of() : ctx.getRoleCodes();
        if (roleCodes.contains(SUPER_ADMIN)) { return wrapper; }
        if (!roleCodes.isEmpty()) {
            boolean anyAll = roleService.list(new DefaultQueryWrapper<UmsRole>()
                            .in("role_code", roleCodes))
                    .stream().anyMatch(r -> DataScopeEnum.ALL.equals(r.getDataScope()));
            if (anyAll) { return wrapper; }
        }
        Long userId = ctx == null ? null : ctx.getUserId();
        if (userId != null) { wrapper.eq(ownerColumn, userId); }
        return wrapper;
    }
}
```

- [ ] **Step 3: 跑测试通过 + 编译 + Commit**

Run: `D:\Tool\apache-maven-3.8.8\bin\mvn test -Dtest=DataScopeBuilderTest` → PASS
Run: `D:\Tool\apache-maven-3.8.8\bin\mvn clean compile -q` → BUILD SUCCESS

```bash
git add src/main/java/com/sanye/strategy/infrastructure/security/DataScopeBuilder.java src/test/java/com/sanye/strategy/infrastructure/security/DataScopeBuilderTest.java
git commit -m "feat(rbac): DataScopeBuilder 数据权限 1/2 级（方案C 实时装配）"
```

---

### Task 11: RbacManageService 门面（事务/内置保护/管理面过滤/自动 evict/导入导出克隆）

**Files:**
- Create: `src/main/java/com/sanye/strategy/application/rbac/RbacManageService.java`
- Create: `src/main/java/com/sanye/strategy/application/rbac/EvictPlan.java`

**Interfaces:**
- Consumes: Task 1/2/3 契约、Task 8 `EvictService`/`EvictTaskRegistry`、Task 9 `OperLogService`、共享 `TransactionTemplate`
- Produces: 门面方法 — Task 12 Controllers 全依赖。写操作成功后自动触发受影响用户 evict（afterCommit 语义）

- [ ] **Step 1: EvictPlan（受影响用户集内聚）**

```java
/** 自动 evict 计划 — 角色维度（角色下用户）或用户维度 */
@Data
@Builder
public class EvictPlan {
    private Long roleId;           // 角色维度优先
    private List<Long> userIds;    // 用户维度
    private String sourceDesc;     // 触发来源（审计）
    private String evictTaskId;    // 异步时有值（同步为 null）

    public long estimateTotal(UmsUserRoleService userRoleService) {
        return roleId != null ? userRoleService.countUserIdsByRoleId(roleId) : (userIds == null ? 0 : userIds.size());
    }
    // 注意：触发侧不再走 collectUserIds（会截断到前 500）——角色维度统一走 evictService.evictRoleUsers 全量分页
}
```

- [ ] **Step 2: RbacManageService 骨架**

```java
/** RBAC 管理门面 — 管理写操作编排（事务/内置保护/管理面过滤/自动 evict/审计）
 * <p>设计模式（门面）：角色=管理面用例编排；优点=跨表事务与自动踢人收口一处、Controller 瘦；
 * 缺点=门面体积随操作增长（按需拆分）。</p> */
@Service
@RequiredArgsConstructor
@Slf4j
public class RbacManageService {

    private static final String SUPER_ADMIN = "SUPER_ADMIN";

    private final UmsRoleService roleService;
    private final UmsPermissionService permissionService;
    private final UmsUserRoleService userRoleService;
    private final UmsRolePermissionService rolePermissionService;
    private final PermissionSyncService syncService;
    private final EvictService evictService;
    private final EvictTaskRegistry evictTaskRegistry;
    private final OperLogService operLogService;
    private final TransactionTemplate transactionTemplate;
    @Value("${rbac.evict-async-threshold:50}") private long evictAsyncThreshold;

    // ============ 通用编排：事务 → afterCommit evict → 审计 ============

    private <T> T manageWrite(Supplier<T> action, EvictPlan plan, OperLogReq logReq) {
        try {
            T result = transactionTemplate.execute(status -> action.get());   // 提交成功才触发 evict
            triggerEvict(plan);
            if (logReq != null) { operLogService.record(logReq); }
            return result;
        } catch (Exception e) {
            if (logReq != null) {   // 业务失败也审计（REQUIRES_NEW 独立事务，失败上下文入 error_msg）
                operLogService.record(OperLogReq.builder()
                        .module(logReq.getModule()).action(logReq.getAction())
                        .desc(logReq.getDesc()).type(logReq.getType())
                        .success(false).errorMsg(e.getMessage()).build());
            }
            throw e;
        }
    }

    private void triggerEvict(EvictPlan plan) {
        if (plan == null) { return; }
        try {
            long total = plan.estimateTotal(userRoleService);
            Supplier<Integer> evict = () -> plan.getRoleId() != null
                    ? evictService.evictRoleUsers(plan.getRoleId())
                    : evictService.evictUsers(plan.getUserIds());
            if (total <= evictAsyncThreshold) {
                int kicked = evict.execute();
                plan.setEvictTaskId(null);
                evictLog(plan, kicked, null, true, null);
            } else {
                String taskId = evictTaskRegistry.submit(evict, plan.getSourceDesc());
                plan.setEvictTaskId(taskId);
                evictLog(plan, -1, taskId, true, null);
            }
        } catch (Exception e) {
            // 权限变更已提交生效，仅踢人延迟——审计失败状态，人工可用 evict 接口补偿
            log.error("自动 evict 失败: 来源={}", plan.getSourceDesc(), e);
            evictLog(plan, -1, null, false, e.getMessage());
        }
    }

    /** evict 审计（同步/异步/失败统一入口；重试在 EvictService.revokeWithRetry） */
    private void evictLog(EvictPlan plan, int kicked, String taskId, boolean success, String errorMsg) {
        String desc = "自动踢人 来源=" + plan.getSourceDesc()
                + (plan.getRoleId() != null ? " 角色ID=" + plan.getRoleId() : "")
                + (plan.getUserIds() != null ? " 用户数=" + plan.getUserIds().size() : "")
                + " 踢中会话=" + (kicked < 0 ? "异步" : kicked)
                + (taskId != null ? " taskId=" + taskId : "");
        operLogService.record(OperLogReq.builder()
                .module("rbac").action("evict").desc(desc).type(OperTypeEnum.EVICT_USER)
                .success(success).errorMsg(errorMsg).build());
    }
}
```

- [ ] **Step 3: 门面方法清单（各写操作按模板实现）**

```java
// ============ 角色 ============
public void createRole(RoleDTO dto) {
    if (roleService.count(new DefaultQueryWrapper<UmsRole>().eq("role_code", dto.getRoleCode())) > 0) {
        throw new BizException(ResultCode.CONFLICT, "角色编码已存在");
    }
    UmsRole role = new UmsRole();
    BeanCopyUtils.copy(dto, role);
    role.setStatus(RoleStatusEnum.NORMAL);
    role.setIsBuiltIn(YesNoEnum.NO);
    manageWrite(() -> { roleService.insert(role); return null; }, null,
            OperLogReq.builder().module("rbac").action("createRole").desc("新增角色 " + dto.getRoleCode())
                    .type(OperTypeEnum.CREATE).success(true).build());
}

public void updateRole(Long id, RoleDTO dto) {
    UmsRole role = roleService.getById(id);
    if (role == null) { throw new BizException(ResultCode.NOT_FOUND); }
    if (YesNoEnum.YES.equals(role.getIsBuiltIn())
            && !dto.getRoleCode().equals(role.getRoleCode())) {
        throw new BizException(ResultCode.CONFLICT, "内置角色禁改 role_code");
    }
    BeanCopyUtils.copy(dto, role, "id", "isBuiltIn", "deleted", "createTime", "updateTime");  // 非内置可改 role_code（内置由上方守卫拦截）
    manageWrite(() -> { roleService.updateById(role); return null; }, null,
            OperLogReq.builder().module("rbac").action("updateRole").desc("修改角色 " + id)
                    .type(OperTypeEnum.UPDATE).success(true).build());
}

public void deleteRole(Long id) {
    UmsRole role = roleService.getById(id);
    if (role == null) { throw new BizException(ResultCode.NOT_FOUND); }
    if (YesNoEnum.YES.equals(role.getIsBuiltIn())) { throw new BizException(ResultCode.CONFLICT, "内置角色不允许删除"); }
    if (userRoleService.countUserIdsByRoleId(id) > 0) { throw new BizException(ResultCode.CONFLICT, "角色已关联用户，先解绑"); }
    manageWrite(() -> { roleService.deleteById(id); rolePermissionService.revokeByRoleId(id); return null; }, null,
            OperLogReq.builder().module("rbac").action("deleteRole").desc("删除角色 " + id)
                    .type(OperTypeEnum.DELETE).success(true).build());
}

public void updateRoleStatus(Long id, RoleStatusEnum status) {
    UmsRole role = roleService.getById(id);
    if (role == null) { throw new BizException(ResultCode.NOT_FOUND); }
    if (YesNoEnum.YES.equals(role.getIsBuiltIn()) && SUPER_ADMIN.equals(role.getRoleCode())
            && RoleStatusEnum.DISABLED.equals(status)) {
        throw new BizException(ResultCode.CONFLICT, "SUPER_ADMIN 不允许停用");
    }
    role.setStatus(status);
    EvictPlan plan = EvictPlan.builder().roleId(id).sourceDesc("角色" + (RoleStatusEnum.NORMAL.equals(status) ? "启用" : "停用") + " roleId=" + id).build();
    manageWrite(() -> { roleService.updateById(role); return null; }, plan,
            OperLogReq.builder().module("rbac").action("updateRoleStatus").desc("角色 " + id + " 状态→" + status.getCode())
                    .type(OperTypeEnum.UPDATE).success(true).build());
}

public Long cloneRole(Long id) {
    UmsRole src = roleService.getById(id);
    if (src == null) { throw new BizException(ResultCode.NOT_FOUND); }
    UmsRole copy = BeanCopyUtils.copy(src, UmsRole.class, "id", "roleCode", "createTime", "updateTime", "deleted");
    String base = src.getRoleCode();
    if (base.length() > 32) { base = base.substring(0, 32); }   // 超长截断源段
    String code; int seq = 1;
    do { code = base + "_COPY_" + seq++; }                       // 截断后查重保证唯一
    while (roleService.count(new DefaultQueryWrapper<UmsRole>().eq("role_code", code)) > 0);
    copy.setRoleCode(code);
    copy.setRoleName(src.getRoleName() + "_副本");
    copy.setStatus(RoleStatusEnum.NORMAL);
    copy.setIsBuiltIn(YesNoEnum.NO);
    Long newId = manageWrite(() -> {
        roleService.insert(copy);
        rolePermissionService.grantBatch(copy.getId(), rolePermissionService.getPermissionIdsByRoleId(id), UserContext.get().getUserId());
        return copy.getId();
    }, null, OperLogReq.builder().module("rbac").action("cloneRole").desc("克隆角色 " + id + " → " + code)
            .type(OperTypeEnum.CREATE).success(true).build());
    return newId;
}
public List<Map<String, Object>> exportRoles() {
    List<UmsRole> roles = roleService.list(new DefaultQueryWrapper<UmsRole>());
    List<Map<String, Object>> out = new ArrayList<>();
    for (UmsRole r : roles) {
        Map<String, Object> m = new LinkedHashMap<>();
        m.put("roleCode", r.getRoleCode());
        m.put("roleName", r.getRoleName());
        m.put("dataScope", r.getDataScope() == null ? null : r.getDataScope().getCode());
        m.put("sortOrder", r.getSortOrder());
        m.put("remark", r.getRemark());
        m.put("permissionCodes", rolePermissionService.getPermissionCodesByRoleId(r.getId()));
        out.add(m);
    }
    return out;
}
public SyncReport importRoles(String json, boolean overwrite) {
    // 按 role_code 匹配：已存在跳过 / overwrite 覆盖（权限绑定变化 → evict 该角色下用户）
    // JSON 解析用 Boot4 默认 Jackson 3（tools.jackson.databind.ObjectMapper）——与 MP JacksonTypeHandler 的 Jackson2 不混用
    List<RoleImportItem> items = objectMapper.readValue(json, objectMapper.getTypeFactory().constructCollectionType(List.class, RoleImportItem.class));
    SyncReport report = new SyncReport();
    Set<String> ignoredCodes = new LinkedHashSet<>();
    for (RoleImportItem it : items) {
        UmsRole role = roleService.getOne(new DefaultQueryWrapper<UmsRole>().eq("role_code", it.getRoleCode()));
        if (role != null && !overwrite) { report.getDeprecated().add("跳过已存在: " + it.getRoleCode()); continue; }
        // 未注册权限码 → 忽略该项并告警（不整体失败）
        List<Long> validPermIds = new ArrayList<>();
        for (String code : it.getPermissionCodes()) {
            UmsPermission p = permissionService.getOne(new DefaultQueryWrapper<UmsPermission>().eq("permission_code", code));
            if (p == null) { ignoredCodes.add(code); continue; }
            validPermIds.add(p.getId());
        }
        if (role == null) {
            UmsRole nr = new UmsRole();
            nr.setRoleCode(it.getRoleCode()); nr.setRoleName(it.getRoleName());
            nr.setDataScope(DataScopeEnum.SELF); nr.setStatus(RoleStatusEnum.NORMAL);
            nr.setIsBuiltIn(YesNoEnum.NO); nr.setSortOrder(it.getSortOrder() == null ? 0 : it.getSortOrder());
            nr.setRemark(it.getRemark());
            Long nrId = manageWrite(() -> {
                roleService.insert(nr);
                if (!validPermIds.isEmpty()) { rolePermissionService.grantBatch(nr.getId(), validPermIds, UserContext.get().getUserId()); }
                return nr.getId();
            }, null, OperLogReq.builder().module("rbac").action("importRoles").desc("导入角色 " + it.getRoleCode())
                    .type(OperTypeEnum.IMPORT).success(true).build());
            report.getAdded().add(it.getRoleCode());
        } else if (overwrite) {
            Set<Long> before = new HashSet<>(rolePermissionService.getPermissionIdsByRoleId(role.getId()));
            boolean changed = !before.equals(new HashSet<>(validPermIds));
            EvictPlan plan = changed
                    ? EvictPlan.builder().roleId(role.getId()).sourceDesc("角色导入覆盖 roleId=" + role.getId()).build()
                    : null;
            manageWrite(() -> {
                rolePermissionService.revokeByRoleId(role.getId());
                if (!validPermIds.isEmpty()) { rolePermissionService.grantBatch(role.getId(), validPermIds, UserContext.get().getUserId()); }
                return null;
            }, plan, OperLogReq.builder().module("rbac").action("importRoles").desc("覆盖导入角色 " + it.getRoleCode() + " 权限变化=" + changed)
                    .type(OperTypeEnum.IMPORT).success(true).build());
            report.getRevived().add(it.getRoleCode());
        }
    }
    report.setIgnored(new ArrayList<>(ignoredCodes));
    if (!ignoredCodes.isEmpty()) { log.warn("角色导入忽略未注册权限码: {}", ignoredCodes); }
    return report;
}
// ============ 权限资源 ============
public void createPermission(PermissionDTO dto) {
    if (dto.getPermissionCode() != null && !dto.getPermissionCode().isBlank()
            && permissionService.count(new DefaultQueryWrapper<UmsPermission>().eq("permission_code", dto.getPermissionCode())) > 0) {
        throw new BizException(ResultCode.CONFLICT, "权限码已存在");
    }
    UmsPermission p = new UmsPermission();
    BeanCopyUtils.copy(dto, p);
    p.setStatus(RoleStatusEnum.NORMAL);
    p.setIsBuiltIn(YesNoEnum.NO);
    manageWrite(() -> { permissionService.insert(p); return null; }, null,
            OperLogReq.builder().module("rbac").action("createPermission").desc("新增权限 " + dto.getPermissionCode())
                    .type(OperTypeEnum.CREATE).success(true).build());
}
public void updatePermission(Long id, PermissionDTO dto) {
    UmsPermission p = permissionService.getById(id);
    if (p == null) { throw new BizException(ResultCode.NOT_FOUND); }
    // permissionCode 禁改（改码 = 换新资源，防角色绑定引用断裂）；内置资源防篡改
    if (YesNoEnum.YES.equals(p.getIsBuiltIn())) { throw new BizException(ResultCode.CONFLICT, "内置资源禁改"); }
    BeanCopyUtils.copy(dto, p, "id", "permissionCode", "isBuiltIn", "deleted", "createTime", "updateTime");
    manageWrite(() -> { permissionService.updateById(p); return null; }, null,
            OperLogReq.builder().module("rbac").action("updatePermission").desc("修改权限 " + id)
                    .type(OperTypeEnum.UPDATE).success(true).build());
}
public void deletePermission(Long id) {
    UmsPermission p = permissionService.getById(id);
    if (p == null) { throw new BizException(ResultCode.NOT_FOUND); }
    if (rolePermissionService.countByPermissionId(id) > 0) { throw new BizException(ResultCode.CONFLICT, "权限已绑定角色，先解绑"); }
    manageWrite(() -> { permissionService.deleteById(id); return null; }, null,
            OperLogReq.builder().module("rbac").action("deletePermission").desc("删除权限 " + id)
                    .type(OperTypeEnum.DELETE).success(true).build());
}
public void updatePermissionStatus(Long id, RoleStatusEnum status) {
    UmsPermission p = permissionService.getById(id);
    if (p == null) { throw new BizException(ResultCode.NOT_FOUND); }
    p.setStatus(status);
    List<Long> roleIds = rolePermissionService.getRoleIdsByPermissionId(id);
    List<Long> userIds = collectActiveUserIds(roleIds);   // 停用/启用对称：都踢（重登同步新快照）
    EvictPlan plan = userIds.isEmpty() ? null
            : EvictPlan.builder().userIds(userIds).sourceDesc("权限" + (RoleStatusEnum.NORMAL.equals(status) ? "启用" : "停用") + " permId=" + id).build();
    manageWrite(() -> { permissionService.updateById(p); return null; }, plan,
            OperLogReq.builder().module("rbac").action("updatePermissionStatus").desc("权限 " + id + " 状态→" + status.getCode())
                    .type(OperTypeEnum.UPDATE).success(true).build());
}
public SyncReport syncPermissions(boolean dryRun) {
    SyncReport r = syncService.sync(dryRun);
    if (!dryRun && !r.getDeprecated().isEmpty()) {
        // 残留停用（对称于权限停用）→ 反查绑定角色 → 角色下用户 evict
        Set<Long> userIds = new LinkedHashSet<>();
        for (String code : r.getDeprecated()) {
            UmsPermission p = permissionService.getOne(new DefaultQueryWrapper<UmsPermission>().eq("permission_code", code));
            if (p != null) { userIds.addAll(collectActiveUserIds(rolePermissionService.getRoleIdsByPermissionId(p.getId()))); }
        }
        if (!userIds.isEmpty()) { evictService.evictUsers(userIds); }
    }
    return r;
}
// ============ 用户-角色 ============
public void replaceUserRoles(Long userId, List<UserRoleAssignDTO> assigns) {
    EvictPlan plan = EvictPlan.builder().userIds(List.of(userId)).sourceDesc("用户角色覆盖 userId=" + userId).build();
    manageWrite(() -> {
        userRoleService.replaceRoles(userId, toEntities(assigns), UserContext.get().getUserId());
        return null;
    }, plan, auditLog("replaceUserRoles", "用户 " + userId + " 角色覆盖 → " + assignDesc(assigns), OperTypeEnum.GRANT));
}
public void assignRolesBatch(List<Long> userIds, Long roleId, LocalDateTime begin, LocalDateTime end) {
    if (begin != null && end != null && !begin.isBefore(end)) { throw new BizException(ResultCode.BAD_REQUEST, "begin 必须早于 end"); }
    EvictPlan plan = EvictPlan.builder().userIds(userIds).sourceDesc("批量授角色 roleId=" + roleId).build();
    manageWrite(() -> { for (Long uid : userIds) { userRoleService.assignRole(uid, roleId, UserContext.get().getUserId()); } return null; },
            plan, auditLog("assignRolesBatch", "批量授角色 roleId=" + roleId + " 用户数=" + userIds.size(), OperTypeEnum.GRANT));
}
public boolean removeUserRole(Long userId, Long roleId) {
    EvictPlan plan = EvictPlan.builder().userIds(List.of(userId)).sourceDesc("解绑角色 userId=" + userId + " roleId=" + roleId).build();
    return manageWrite(() -> userRoleService.removeUserRole(userId, roleId), plan, auditLog("removeUserRole", "解绑", OperTypeEnum.DELETE));
}
public boolean renewUserRole(Long userId, Long roleId, LocalDateTime endTime) {
    EvictPlan plan = EvictPlan.builder().userIds(List.of(userId)).sourceDesc("续期 userId=" + userId + " roleId=" + roleId).build();
    return manageWrite(() -> userRoleService.renew(userId, roleId, endTime), plan,
            OperLogReq.builder().module("rbac").action("renewUserRole").desc("续期 userId=" + userId + " roleId=" + roleId + " end=" + endTime)
                    .type(OperTypeEnum.UPDATE).success(true).build());
}
public int renewBatch(List<Long> bindIds, LocalDateTime endTime) {
    // 按绑定行 ID 反查 userId（renewById 走行主键，契约与单角色续期 renew(userId,roleId) 分离）
    Set<Long> userIds = new LinkedHashSet<>();
    for (Long bindId : bindIds) {
        UmsUserRole ur = userRoleService.getById(bindId);
        if (ur != null) { userIds.add(ur.getUserId()); }
    }
    EvictPlan plan = EvictPlan.builder().userIds(new ArrayList<>(userIds)).sourceDesc("批量续期 bindIds=" + bindIds.size()).build();
    return manageWrite(() -> {
        int n = 0;
        for (Long bindId : bindIds) { if (userRoleService.renewById(bindId, endTime)) { n++; } }
        return n;
    }, plan, OperLogReq.builder().module("rbac").action("renewBatch").desc("批量续期 " + bindIds.size() + " 条绑定").type(OperTypeEnum.UPDATE).success(true).build());
}
// ============ 角色-权限 ============
public void replaceRolePermissions(Long roleId, List<Long> permissionIds) {
    EvictPlan plan = EvictPlan.builder().roleId(roleId).sourceDesc("角色权限覆盖 roleId=" + roleId).build();
    manageWrite(() -> {
        rolePermissionService.revokeByRoleId(roleId);
        rolePermissionService.grantBatch(roleId, permissionIds, UserContext.get().getUserId());
        return null;
    }, plan, auditLog("replaceRolePermissions", "角色 " + roleId + " 权限覆盖 → " + permissionIds.size() + " 条", OperTypeEnum.GRANT));
}
public void grantRolePermissions(Long roleId, List<Long> permissionIds) {
    EvictPlan plan = EvictPlan.builder().roleId(roleId).sourceDesc("角色增量授权 roleId=" + roleId).build();
    manageWrite(() -> { rolePermissionService.grantBatch(roleId, permissionIds, UserContext.get().getUserId()); return null; },
            plan, OperLogReq.builder().module("rbac").action("grantRolePermissions").desc("角色 " + roleId + " 增量授权 " + permissionIds.size() + " 条")
                    .type(OperTypeEnum.GRANT).success(true).build());
}
public void revokeRolePermissions(Long roleId, List<Long> permissionIds) {
    EvictPlan plan = EvictPlan.builder().roleId(roleId).sourceDesc("角色回收权限 roleId=" + roleId).build();
    manageWrite(() -> { for (Long pid : permissionIds) { rolePermissionService.revoke(roleId, pid); } return null; },
            plan, OperLogReq.builder().module("rbac").action("revokeRolePermissions").desc("角色 " + roleId + " 回收权限 " + permissionIds.size() + " 条")
                    .type(OperTypeEnum.DELETE).success(true).build());
}
// ============ 调试/主动失效（受 debug 开关） ============
public int evictUser(Long userId) { return evictService.evictUsers(List.of(userId)); }
public EvictTaskVO evictBatch(Long roleId, String mode) {
    if ("async".equals(mode)) {
        String taskId = evictTaskRegistry.submit(() -> evictService.evictRoleUsers(roleId), "手动批量踢 roleId=" + roleId);
        return evictTaskRegistry.get(taskId);
    }
    int kicked = evictService.evictRoleUsers(roleId);   // 同步小量
    EvictTaskVO vo = new EvictTaskVO();
    vo.setSourceDesc("手动批量踢(同步) roleId=" + roleId);
    vo.setStatus("SUCCESS"); vo.setKicked(kicked);
    vo.setCreatedAt(LocalDateTime.now()); vo.setDoneAt(LocalDateTime.now());
    return vo;
}
// ============ 辅助 ============
/** 角色集 → 活动用户并集（offset 分页去重；权限停用/启用/导入覆盖反查 evict 用） */
private List<Long> collectActiveUserIds(List<Long> roleIds) {
    Set<Long> users = new LinkedHashSet<>();
    for (Long rid : roleIds) {
        long offset = 0; List<Long> ids;
        do {
            ids = userRoleService.listActiveUserIdsByRoleId(rid, offset, 500);
            offset += ids.size();
            users.addAll(ids);
        } while (ids.size() == 500);
    }
    return new ArrayList<>(users);
}
// ============ 管理面过滤（DAO 统一封装，禁止复用业务表） ============
public IWrapper<UmsRole> applyManageScope(IWrapper<UmsRole> wrapper) {
    UserContext ctx = UserContext.get();
    if (ctx == null || ctx.getRoleCodes().contains(SUPER_ADMIN)) { return wrapper; }
    // 自定义 IWrapper 无 and(Consumer)——OR 组用 nested（CLAUDE.md 约定）
    return wrapper.nested(w -> w.isNull("create_user_id").or().eq("create_user_id", ctx.getUserId()));
}
```

> `BeanCopyUtils.copy(dto, role, ignoreFields...)` — 检查 `BeanCopyUtils` 签名是否支持忽略字段；若不支持，手工 set 字段（实现时按实际 API 定）。
> **importRoles 依赖：** `RbacManageService` 注入 `private final ObjectMapper objectMapper;`（`tools.jackson.databind.ObjectMapper`，Boot4 默认 Jackson 3）；`RoleImportItem` 为 `interfaces/rbac/dto` 下 `@Data` 类：`{roleCode, roleName, sortOrder, remark, List<String> permissionCodes}`。

- [ ] **Step 2: 编译验证 + Commit**

Run: `D:\Tool\apache-maven-3.8.8\bin\mvn clean compile -q` → BUILD SUCCESS（缺的 DTO/VO 在 Task 12 建；此处门面可先用内联参数或 Task 12 DTO 前留 TODO？——**改顺序：Task 12 DTO/VO 与 Task 11 同批编译**，或 Task 11 门面先用简单类型，Task 12 再替换。实现时先建 DTO/VO 骨架再写门面，合并为一次提交）

> **执行提示：** Task 11 与 Task 12 紧密耦合（门面签名 = Controller 入参），实际实现合并为「先 DTO/VO → 再门面 → 再 Controller」三步连做，提交可拆两笔（`feat(rbac): RbacManageService 门面` + `feat(rbac): RBAC 管理 API`）。

```bash
git add src/main/java/com/sanye/strategy/application/rbac/RbacManageService.java src/main/java/com/sanye/strategy/application/rbac/EvictPlan.java src/main/java/com/sanye/strategy/interfaces/rbac
git commit -m "feat(rbac): RbacManageService 门面 — 事务/内置保护/管理面过滤/自动evict/审计"
```

---

### Task 12: RBAC Controllers + DTO/VO + 权限码 + debug 开关

**Files:**
- Create: `src/main/java/com/sanye/strategy/interfaces/rbac/controller/RbacRoleController.java`
- Create: `src/main/java/com/sanye/strategy/interfaces/rbac/controller/RbacPermissionController.java`
- Create: `src/main/java/com/sanye/strategy/interfaces/rbac/controller/RbacUserRoleController.java`
- Create: `src/main/java/com/sanye/strategy/interfaces/rbac/controller/RbacRolePermissionController.java`
- Create: `src/main/java/com/sanye/strategy/interfaces/rbac/controller/RbacQueryController.java`
- Create: `src/main/java/com/sanye/strategy/interfaces/rbac/controller/RbacDebugController.java`
- Create: `src/main/java/com/sanye/strategy/interfaces/rbac/dto/*.java`
- Create: `src/main/java/com/sanye/strategy/interfaces/rbac/vo/*.java`
- Create: `src/main/java/com/sanye/strategy/application/rbac/RbacAuthzService.java`
- Modify: `src/main/resources/application.yaml`

**Interfaces:**
- Consumes: `RbacManageService`（Task 11）、`EvictTaskRegistry`（Task 8）、`UmsPermissionService`（Task 1，菜单树用）
- Produces: 全部 `/rbac/**` 端点（spec API 面表）

**权限码落位（spec 清单）：**

| Controller | 权限码 | 挂载 |
|-----------|--------|------|
| RbacRoleController | `system:role:manage` | 类级 |
| RbacPermissionController | `system:permission:manage` | 类级 |
| RbacUserRoleController | `system:user:role:manage` | 类级 |
| RbacRolePermissionController | `system:role:assign` | 类级 |
| RbacQueryController | `@NoPermissionRequired` | 类级（当前用户） |
| RbacDebugController | `system:rbac:debug:manage` + `@ConditionalOnProperty(rbac.debug-enabled=true)` | 类级 |

- [ ] **Step 1: DTO/VO 骨架**

`RoleDTO{roleCode, roleName, dataScope(Integer), sortOrder, remark}` / `RoleQueryDTO{roleCode, roleName, status, page, size, sortField, sortOrder}`（组合分页）/ `RoleVO` / `PermissionDTO{parentId, permissionName, permissionType, permissionCode, routePath, componentPath, apiMethod, apiPath, icon, sortOrder, isVisible, remark}` / `PermissionQueryDTO` / `PermissionVO`（树节点含 `List<PermissionVO> children`）/ `UserRoleAssignDTO{roleId, beginTime, endTime}` / `RolePermissionAssignDTO{permissionIds}` / `EvictTaskVO`（Task8）。DTO 加 `@Valid` 校验（roleCode 非空、begin<end）。

- [ ] **Step 2: RbacAuthzService（调试/生效权限）**

```java
/** 授权查询服务 — 调试 + 当前用户查询（受 rbac.debug-enabled 开关） */
@Service
@RequiredArgsConstructor
public class RbacAuthzService {
    private final UmsUserRoleService userRoleService;
    private final UmsRolePermissionService rolePermissionService;
    private final UmsRoleService roleService;   // effectivePermissions 反查 roleId 用

    /** 用户实际生效权限码集（合并多角色去重，过滤禁用/过期——联表已过滤） */
    public Set<String> effectivePermissions(Long userId) {
        List<String> roleCodes = userRoleService.getRoleCodesByUserId(userId);
        if (roleCodes.contains("SUPER_ADMIN")) { return Set.of("*"); }
        Set<String> codes = new LinkedHashSet<>();
        for (String roleCode : roleCodes) {
            UmsRole role = roleService.getOne(new DefaultQueryWrapper<UmsRole>().eq("role_code", roleCode));
            if (role != null) { codes.addAll(rolePermissionService.getPermissionCodesByRoleId(role.getId())); }
        }
        return codes;
    }
    /** 用户是否拥有权限码 */
    public boolean checkPermission(Long userId, String permissionCode) {
        PermissionCodeValidator.validate(permissionCode);
        return effectivePermissions(userId).contains(permissionCode);
    }
}
```

> 当前用户 `getMyPermissions` 用 `UserContext.getPermCodes()` 零 DB（JWT 快照）；`effectivePermissions` 调试用实时联表。

- [ ] **Step 3: Controllers**

按 spec API 面表逐端点实现（RESTful，挂权限码）。`RbacQueryController`：

```java
/** 当前用户权限查询 — @NoPermissionRequired（仅需登录，前端菜单/按钮渲染） */
@RestController
@RequestMapping("/rbac/my")
@NoPermissionRequired
@RequiredArgsConstructor
public class RbacQueryController {
    private final UmsPermissionService permissionService;

    @GetMapping("/permissions")
    public R<List<String>> myPermissions() {
        UserContext ctx = UserContext.get();
        return R.ok(ctx == null ? List.of() : ctx.getPermCodes());
    }

    @GetMapping("/menu-tree")
    public R<List<PermissionVO>> myMenuTree() {
        UserContext ctx = UserContext.get();
        Set<String> perms = ctx == null ? Set.of() : new HashSet<>(ctx.getPermCodes());
        List<PermissionVO> all = permissionService.list(new DefaultQueryWrapper<UmsPermission>()
                        .eq("status", RoleStatusEnum.NORMAL.getCode())
                        .eq("is_visible", YesNoEnum.YES.getCode())
                        .orderByAsc("sort_order"))
                .stream().map(this::toTreeVO).toList();
        // 按钮级按 perms 快照过滤；目录/菜单保留（数据权限无关，纯功能权限 JWT 快照）
        List<PermissionVO> filtered = all.stream()
                .filter(n -> n.getPermissionType() != PermissionTypeEnum.BUTTON
                        || perms.contains(n.getPermissionCode()))
                .toList();
        Map<Long, List<PermissionVO>> byParent = filtered.stream()
                .collect(Collectors.groupingBy(PermissionVO::getParentId));
        filtered.forEach(n -> n.setChildren(byParent.getOrDefault(n.getId(), List.of())));
        return R.ok(byParent.getOrDefault(0L, List.of()));
    }
}
```
> `toTreeVO(UmsPermission)` 私有转换：`BeanCopyUtils.copy(p, PermissionVO.class)` + `setChildren(new ArrayList<>())`。`PermissionVO` 含 `children` 字段（Task 12 DTO/VO 骨架已定义）。
```

`RbacDebugController`：类级 `@RequiresPermission("system:rbac:debug:manage")`。**不用** `@ConditionalOnProperty`（关闭须 403 非 404，防暴露接口存在，spec 硬约束）——类常装配，每端点 `ensureEnabled()` 校验 `rbac.debug-enabled`，关闭时抛 403 + 访问日志。含 `effective-permissions` / `check` / `evict` / `evict-batch` / `evict-tasks/{taskId}`：

```java
/** 调试/主动失效 — 受 rbac.debug-enabled 开关（生产关）。自动 evict 属管理写链路不受此开关控制 */
@RestController
@RequestMapping("/rbac")
@RequiresPermission("system:rbac:debug:manage")
@RequiredArgsConstructor
@Slf4j
public class RbacDebugController {
    private final RbacManageService rbacManageService;
    private final RbacAuthzService rbacAuthzService;
    private final EvictTaskRegistry evictTaskRegistry;
    @Value("${rbac.debug-enabled:false}") private boolean debugEnabled;

    private void ensureEnabled() {
        if (!debugEnabled) {
            UserContext ctx = UserContext.get();
            log.warn("RBAC 调试接口被访问但 debug-enabled=false, operator={}", ctx == null ? "?" : ctx.getUserId());
            throw new BizException(ResultCode.FORBIDDEN, "调试接口未启用");
        }
    }
    @GetMapping("/users/{id}/effective-permissions") public R<Set<String>> effective(@PathVariable Long id) { ensureEnabled(); return R.ok(rbacAuthzService.effectivePermissions(id)); }
    @GetMapping("/users/{id}/check") public R<Boolean> check(@PathVariable Long id, @RequestParam String permission) { ensureEnabled(); return R.ok(rbacAuthzService.checkPermission(id, permission)); }
    @PostMapping("/users/{id}/evict") public R<Integer> evictUser(@PathVariable Long id) { ensureEnabled(); return R.ok(rbacManageService.evictUser(id)); }
    @PostMapping("/evict-batch") public R<EvictTaskVO> evictBatch(@RequestParam Long roleId, @RequestParam(defaultValue = "async") String mode) { ensureEnabled(); return R.ok(rbacManageService.evictBatch(roleId, mode)); }
    @GetMapping("/evict/tasks/{taskId}") public R<EvictTaskVO> task(@PathVariable String taskId) { ensureEnabled(); return R.ok(evictTaskRegistry.get(taskId)); }
}
```

- [ ] **Step 4: application.yaml 配置**

```yaml
rbac:
  debug-enabled: false
  evict-async-threshold: 50
  expiry-scan-interval-minutes: 1
  sync-on-startup: true
```

- [ ] **Step 5: 编译 + 启动冒烟（手动验证）**

Run: `D:\Tool\apache-maven-3.8.8\bin\mvn clean package -DskipTests -q` → BUILD SUCCESS
Run: `D:\Tool\apache-maven-3.8.8\bin\mvn spring-boot:run`（dev）
冒烟：
1. `POST /auth/register`（新用户）→ 200
2. `GET /rbac/my/permissions`（带 Bearer）→ 200 空集（NORMAL_USER 无权限点）
3. `POST /rbac/roles` → 403（无 `system:role:manage`）
4. 日志确认启动权限同步执行（新增 system:* 权限码）

- [ ] **Step 6: Commit**

```bash
git add src/main/java/com/sanye/strategy/interfaces/rbac src/main/java/com/sanye/strategy/application/rbac/RbacAuthzService.java src/main/resources/application.yaml
git commit -m "feat(rbac): RBAC 管理 API 5 Controller + DTO/VO + 权限码 + debug 开关"
```

---

### Task 13: 角色到期定时任务（只扫已过期）

**Files:**
- Create: `src/main/java/com/sanye/strategy/infrastructure/config/SchedulingConfig.java`
- Create: `src/main/java/com/sanye/strategy/application/rbac/RbacRoleExpiryScheduler.java`

**Interfaces:**
- Consumes: Task 3 `listExpired`、Task 8 `EvictService`、Task 9 `OperLogService`/`OperTypeEnum`
- Produces: 周期踢已到期角色用户（收回过期授权）

- [ ] **Step 1: 实现**

```java
package com.sanye.strategy.application.rbac;

/** 角色到期自动 evict — 只扫已过期绑定（end_time <= now），踢受影响用户收回过期授权
 * <p>与功能权限（JWT 快照）对比：本任务兜底「token 有效期内角色到期」安全窗口。
 * 不提前扫（提前踢会在角色真正到期前反复踢——重登新 token 仍含该角色，体验受损）；
 * begin_time 到达不踢（缺权限安全默认，refresh/重登自然获得）。
 * 周期幂等：全量扫已过期，会话黑名单 TTL 取剩余，已过期会话跳过，无重复踢实际效果。
 * 集群：本期单实例；多实例部署须分布式锁保证同刻仅一实例执行（TODO）。
 * 审计：批量记录一条摘要（oper_type=EVICT_USER，来源「角色到期定时任务」）。</p> */
@Component
@RequiredArgsConstructor
@Slf4j
public class RbacRoleExpiryScheduler {
    private static final int BATCH_SIZE = 500;
    private final UmsUserRoleService userRoleService;
    private final EvictService evictService;
    private final OperLogService operLogService;

    // fixedDelayString + timeUnit=MINUTES：Spring 6.1+ 支持 String 变体指定 timeUnit（验证通过后沿用；
    // 若不支持则 fixedDelayString 退化为 1ms 每周期——届时改用 fixedDelay = 60_000L）
    @Scheduled(fixedDelayString = "${rbac.expiry-scan-interval-minutes:1}", timeUnit = TimeUnit.MINUTES)
    public void scanExpiredRoles() {
        int totalExpired = 0, totalKicked = 0;
        long offset = 0;
        List<UmsUserRole> expired;
        do {
            // offset 分页：全量扫完已过期（无 offset 会每周期反复处理同一批前 500，后续饿死）
            expired = userRoleService.listExpired(LocalDateTime.now(), offset, BATCH_SIZE);
            offset += expired.size();
            if (expired.isEmpty()) { break; }
            Set<Long> userIds = expired.stream().map(UmsUserRole::getUserId).collect(Collectors.toSet());
            totalKicked += evictService.evictUsers(userIds);
            totalExpired += expired.size();
        } while (expired.size() == BATCH_SIZE);
        if (totalExpired == 0) { return; }
        // 批量摘要审计（不逐用户）
        operLogService.record(OperLogReq.builder()
                .module("rbac").action("evict").type(OperTypeEnum.EVICT_USER)
                .desc("角色到期定时任务 已过期绑定=" + totalExpired + " 踢中会话=" + totalKicked)
                .success(true).build());
        log.info("RBAC 角色到期扫描: {} 条已过期绑定，踢中 {} 会话", totalExpired, totalKicked);
    }
}
```

> `UmsUserRole`（Task 3）需 `getUserId()`。`listExpired` 返回 `List<UmsUserRole>`（契约已定义）。

- [ ] **Step 2: SchedulingConfig（@EnableScheduling）**

```java
/** 定时任务配置 — 启用 @Scheduled（集群部署须分布式锁，见 spec 待办） */
@Configuration
@EnableScheduling
public class SchedulingConfig {
}
```

- [ ] **Step 3: 编译 + Commit**

Run: `D:\Tool\apache-maven-3.8.8\bin\mvn clean compile -q` → BUILD SUCCESS

```bash
git add src/main/java/com/sanye/strategy/application/rbac/RbacRoleExpiryScheduler.java src/main/java/com/sanye/strategy/infrastructure/config/SchedulingConfig.java
git commit -m "feat(rbac): 角色到期定时扫描自动踢人（只扫已过期 + 批量摘要审计）"
```

---

### Task 14: 集成测试 + 全量构建 + 文档收尾

**Files:**
- Create: `src/test/java/com/sanye/strategy/application/rbac/RbacManageServiceTest.java`
- Modify: `docs/superpowers/specs/2026-08-14-rbac-design.md`（实现偏离回写）
- Modify: `CLAUDE.md`（包结构补 rbac 包）

**Interfaces:**
- Consumes: 全部任务产物

- [ ] **Step 1: RbacManageService 单测**

```java
// 用例（mock 依赖）：
// updateRoleStatus(停用) → plan 为 roleId 维度 → verify evictService.evictRoleUsers 被调（阈值内同步）
// SUPER_ADMIN 管理面过滤 → 无 eq 条件；普通管理员 → nested(isNull(create_user_id).or().eq(create_user_id, userId))
// 内置角色 delete → 抛 BizException(CONFLICT)
// replaceRolePermissions → verify 事务内 revokeByRoleId + grantBatch
// assignRolesBatch begin>=end → 抛 BizException(BAD_REQUEST)
// 受影响用户数 > 阈值 → evictTaskRegistry.submit 被调（异步）
// replaceUserRoles → verify userRoleService.replaceRoles 在事务内执行
```

- [ ] **Step 2: 全量构建 + 全测试**

Run: `D:\Tool\apache-maven-3.8.8\bin\mvn clean package`
Expected: BUILD SUCCESS（全部单测通过）

- [ ] **Step 3: 文档核对**

- spec「开发注意事项 3」：发布流程强制手动 sync——标注运维步骤（CLAUDE.md 或 README）
- 功能权限 vs 数据权限两模型：确认相关接口 javadoc 已写明
- 管理面过滤 TODO 注释：「禁止复用业务表」已写
- CLAUDE.md 包结构补 `interfaces/rbac`、`application/rbac`、`domain/rbac`

- [ ] **Step 4: Commit**

```bash
git add src/test/java/com/sanye/strategy/application/rbac/RbacManageServiceTest.java docs/superpowers/specs/2026-08-14-rbac-design.md CLAUDE.md
git commit -m "test(rbac): RbacManageService 单测 + 全量构建 + 文档核对"
```

---

## Self-Review（已完成，摘要 + 二次自检修复）

- **Spec 覆盖核对：** 权限数据三件套→T1/T2；UmsUserRole 扩展→T3；鉴权注解+校验→T4；JWT perms+上限→T5；拦截器+注册→T6；扫描注册（新增/复活/残留停用/dry-run）→T7；evict+异步任务→T8；审计→T9；DataScopeBuilder→T10；管理门面（自动evict/内置保护/管理面过滤/导入导出克隆）→T11；Controllers+权限码+debug开关→T12；到期定时（**只扫已过期** + 摘要审计 + 集群TODO）→T13；测试+文档→T14
- **类型一致性：** `UmsUserRoleService.listExpired(now, offset, limit)`（T3）↔ `RbacRoleExpiryScheduler`（T13）；`listActiveUserIdsByRoleId(roleId, offset, limit)`（T3）↔ `EvictService.evictRoleUsers`/`EvictPlan`/`collectActiveUserIds`（T8/11）；`EvictService.evictUsers(Collection<Long>)`（T8）↔ T11/13；`JwtUtil.generateAccessToken` 五参新签名（T5）同步至 issueTokens/refresh；`UmsRolePermissionService.grantBatch(roleId, List<Long>, grantUserId)`（T2）↔ T11；`getRoleIdsByPermissionId`（T2）↔ T11 权限停用/同步反查

### 二次自检（2026-08-14，本次）已修复缺陷

| 缺陷 | 位置 | 修复 |
|------|------|------|
| T4 测试 `rejectsModuleWithColon`：`sys:tem:role:manage` 按段校验**合法**（模块 sys + 资源 tem:role + 操作 manage），断言反了会挂 | T4 Step1 | 改为 `acceptsNestedResourceModule`（断言不抛） |
| T6 测试 `rbacPathWithoutAnnotationIsForbidden`：handlerWithout 的 beanType 带 `@NoPermissionRequired` → 拦截器放行，测试必挂 | T6 Step2 | 拆 PlainController（无注解）供无注解用例；WithoutController 留类级豁免用例 |
| T7 启动 sync(false) 会**停用残留权限**（spec：启动只新增+复活） | T7 Step2/3 | `sync()` 拆 `doSync(dryRun, includeDeprecated)` + `syncStartup()`；runner 调 syncStartup |
| T7 扫描过滤 `AssignableTypeFilter(Controller.class)` 永远不命中（注解接口非具体类父类），方法级注解漏扫 | T7 Step2 | 改 `AnnotationTypeFilter(RestController.class)`（元注解匹配） |
| T5 无 `getAccessTokenTtlSeconds()`，T8 EvictService 引用即编译失败 | T5 Step1 | 补方法 |
| T8 `evictRoleUsers` 分页循环 `off` 死变量，无 offset → 同一批 500 反复踢、501+ 永不踢 | T8 Step3 | 改 offset 分页循环；T3 契约加 offset 参数 |
| T11 `EvictPlan.collectUserIds` 截断到前 500；角色维度 evict 漏用户 | T11 | 删除该方法；triggerEvict 角色维度走 `evictRoleUsers` 全量分页 |
| T11 `applyManageScope` 用 `and(Consumer)`，自定义 IWrapper 无此 API | T11 | 改 `nested(w -> ...)`（CLAUDE.md 约定） |
| T11 `submitSync`/`findRoleIdsByPermission`/`collectUsersByRoleIds` 未定义 | T11 | evictBatch 重写；T2 契约加 `getRoleIdsByPermissionId`；加 `collectActiveUserIds` 辅助 |
| T11 `EVICT_RETRY=3` 死常量，重试未实现（spec P1） | T8/T11 | 重试移入 `EvictService.revokeWithRetry`（指数退避 + 失败上抛）；RbacManageService 审计失败状态 |
| T11 业务失败无审计（spec：REQUIRES_NEW 记录尝试） | T11 manageWrite | try/catch 记录失败审计 |
| T11 `updateRole` copy 忽略 roleCode → 非内置角色也改不了码 | T11 | ignore 列表去掉 roleCode（内置守卫已拦截） |
| T11 renewBatch 按绑定行 ID，契约只有 renew(userId, roleId) | T3/T11 | 契约加 `renewById(bindId, endTime)` + `getById(bindId)` |
| T11 clone/export/import 占位注释（无代码/测试） | T11 | 全部补实码 + importRoles 未注册权限码忽略告警 + SyncReport.ignored |
| T12 debug 关闭返回 404，spec 要求 403 + 访问日志 | T12 Step3 | 去 `@ConditionalOnProperty`，改 `ensureEnabled()` 403 + log.warn |
| T12 myMenuTree 注释占位 | T12 Step3 | 补递归树实码（perms 快照过滤按钮） |
| T13 `listExpired` LIMIT 500 无 offset → 每周期反复处理同一批，后续饿死 | T13 Step1 | offset 分页循环全量扫完 |
| 4 类用 `log` 无 `@Slf4j`（编译失败） | T7/T9/T11/T13 | 补 @Slf4j |
| T8 `EvictService` 缺 `userRoleService` 注入字段 | T8 Step3 | 补字段 |
| DataScopeBuilder 测试「无上下文 → eq」与实现（ctx null 无 userId 不滤）矛盾 | T10 Step1 | 测试口径改：无上下文返回原 wrapper（认证链路必有 ctx） |
| spec 字符集口径两处不一致（`[a-z][a-z0-9:_-]+` vs 按段） | spec | 统一为按段校验 |

### 残余注意（记录，不修）

- T12 四个管理 Controller（role/permission/user-role/role-permission）按 spec API 面表逐端点实现，未逐方法贴码——端点路径/入出参/RbacManageService 方法签名已全定义，属薄 Controller 转发，执行时照 spec 表实现
- `assignRolesBatch` 全量事务原子（spec 写「100/批 + 失败集返回」），事务内全成功或全回滚，失败集由审计 error_msg 呈现——原子性优先，可接受
- T5 `loadPermCodes` 循环内 `getOne` 查角色（N+1）——权限量小可接受；压测后如需批量 `in` 查询优化
- DataScopeBuilder 对 data_scope=3/4/5（未实现级）安全默认「仅本人」（不越权），后续实现时替换
- `assignRole` 对已存在绑定未去重（若 ums_user_role 无唯一键）——T12 覆盖绑定走 replaceRoles（删+插）无此问题；批量授同一角色重复调用需在 Controller 层去重，实现时确认
