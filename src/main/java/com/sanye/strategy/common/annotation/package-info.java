/**
 * <p>
 * 自定义注解包
 * </p>
 * <p>
 * 存放全局通用的自定义注解，如：
 * <ul>
 *   <li>{@code @Log} - 日志注解</li>
 *   <li>{@code @NoAuth} - 免登录注解</li>
 *   <li>{@code @ValidPhone} - 手机号校验注解</li>
 *   <li>{@code @RepeatSubmit} - 防重复提交注解</li>
 * </ul>
 * 本包不包含业务特定的注解。
 * </p>
 *
 * @author 31372
 */
package com.sanye.strategy.common.annotation;
