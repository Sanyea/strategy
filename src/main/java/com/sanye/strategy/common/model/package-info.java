/**
 * <p>
 * 通用数据模型包
 * </p>
 * <p>
 * 存放通用数据模型（DTO/VO/BO），如：
 * <ul>
 *   <li>{@code PageQuery} - 分页参数</li>
 *   <li>{@code PageResult} - 分页结果</li>
 *   <li>通用 VO/DTO/BO</li>
 * </ul>
 * <p>
 * 注意：不存放具体业务表实体类（Entity 应放在对应业务模块）。
 * </p>
 *
 * @author 31372
 */
package com.sanye.strategy.common.model;
