# 本次对话完整记录（K8s 中间件部署）

> 会话时间：2026-09-07 20:46 — 22:00
> 命名空间：`dev`
> 目标集群环境：Kubernetes（kubeadm + containerd + rook-ceph-block 存储 + flannel）
> 节点：master(192.168.254.129) / node1 / node2

本记录完整还原了本次会话中从镜像下载到各中间件（Redis、Elasticsearch、Kibana、RabbitMQ、Milvus、MinIO、etcd）在 dev 命名空间部署、暴露、认证修复的全过程，含部署清单、资源限制、访问方式与问题排查记录。

---

## 一、会话背景与镜像准备

### 1.1 需求来源
用户要求从 `D:\Jetbrains\IntelliJ IDEA 2023.3.8\project\strategy\k8s` 清单中读取各组件版本并下载对应镜像。

### 1.2 镜像下载结论
- Kafka：`apache/kafka:3.7.2`（`bitnami/kafka:3.7` 在 1ms 加速源上返回 404，改用 apache 官方替代）
- RabbitMQ：`rabbitmq:3.13-management`
- Milvus：`milvusdb/milvus:v2.6.18`
- MinIO：`minio/minio:RELEASE.2024-12-18T13-15-44Z`
- etcd：`quay.io/coreos/etcd:v3.5.25`
- Elasticsearch：`docker.elastic.co/elasticsearch/elasticsearch:8.13.4`
- Kibana：`docker.elastic.co/kibana/kibana:8.13.4`
- Redis：`redis:7.2`

### 1.3 镜像源策略
- docker.io 系镜像：客户端无法直连，使用 `docker.1ms.run` 加速源
- etcd：直连 `quay.io`
- Elasticsearch / Kibana：直连 `docker.elastic.co`

### 1.4 导出 tar 包（存放于工作目录）
`apache_kafka_3.7.2_amd64.tar`、`library_rabbitmq_3.13-management_amd64.tar`、`library_redis_7.2_amd64.tar`、`milvusdb_milvus_v2.6.18_amd64.tar`、`minio_minio_RELEASE.2024-12-18T13-15-44Z_amd64.tar`、`coreos_etcd_v3.5.25_amd64.tar`、`elasticsearch_elasticsearch_8.13.4_amd64.tar`、`kibana_kibana_8.13.4_amd64.tar`

> 说明：临时 layer_gzip 缓存文件已清理。

---

## 二、Redis 部署（已完成，前序任务）

- 部署类型：StatefulSet（`redis-dev`）
- 镜像：`redis:7.2`
- 认证：Secret `redis-secret`，AOF 开启，密码 `redis123456`
- 存储：rook-ceph-block PVC `10Gi`
- 资源限制：requests `200m/256Mi`，limits `1C/1Gi`
- 暴露：NodePort `30379`（6379）

---

## 三、Elasticsearch & Kibana 部署

### 3.1 需求
在 dev 命名空间创建 ES 与 Kibana，NodePort 对外暴露 API 与 UI，限制 CPU/内存；ES rook-ceph-block 30G，Kibana 5G。

### 3.2 清单文件
`d:\Agent Work Space\6a9e9ea1b1520745e24f3b87\es-kibana-dev-pvc.yaml`

### 3.3 核心配置

| 组件 | 镜像 | 存储 | 资源限制 | NodePort |
|------|------|------|----------|----------|
| Elasticsearch | elasticsearch:8.13.4 | 30Gi | req 500m/1Gi → lim 2C/2Gi | 31200(http)/31202(transport) |
| Kibana | kibana:8.13.4 | 5Gi | req 200m/512Mi → lim 1C/1Gi | 31201 |

### 3.4 遇到的问题与修复

**问题：ES / Kibana 容器权限错误（EACCES）**
- 现象：无法写入 PVC 挂载目录，日志 `failed to obtain node locks` / `Unable to write to UUID file`，容器 CrashLoopBackOff
- 修复：Deployment 增加 `securityContext.fsGroup: 1000`，使 CSI 挂载目录归属组 ID 1000，容器进程加入该组获得写入权限
- 补充：首次修改后需重新 apply 触发滚动更新（曾出现改了配置但 Pod 未更新的情况）

**问题：Kibana 认证失败**
- 现象：日志 `Unable to retrieve version information from Elasticsearch nodes. security_exception: unable to authenticate user [kibana_system]`
- 根因：ES 8.x 官方镜像仅通过 `ELASTIC_PASSWORD` 自动设置 elastic 用户密码；`KIBANA_SYSTEM_PASSWORD` 只是 Kibana 侧变量，**ES 不会用它对 `kibana_system` 用户改密**，该用户仍是 bootstrap 随机密码，Kibana 用 `kibana123456` 认证必然失败
- 修复：在 ES 容器内用 superuser `elastic` 账号调用安全 API 手动把 `kibana_system` 密码重置为与 Kibana 配置一致的 `kibana123456`：

```bash
curl -s -u elastic:elastic123456 -X POST \
  "http://localhost:9200/_security/user/kibana_system/_password" \
  -H "Content-Type: application/json" \
  -d '{"password":"kibana123456"}'
```

- 曲折点：PowerShell 传参嵌套引号导致 JSON body 丢失（报 `Unrecognized character escape ':'` / `request body is required`），最终通过写脚本文件 → `kubectl cp` 进容器 → 执行的方式绕开引号传递问题
- 验证：`kibana_system:kibana123456` 认证返回 HTTP 200，Kibana pod 恢复 `1/1 Running`，日志不再有 `security_exception`

### 3.5 访问方式
- ES API：`http://节点IP:31200`，账号 `elastic / elastic123456`（带认证返回 200，未带认证返回 401 属预期）
- Kibana UI：`http://节点IP:31201`（返回 200）

---

## 四、RabbitMQ 部署

### 4.1 需求
在 dev 命名空间创建 RabbitMQ，NodePort 对外暴露 API/UI，限制 CPU/内存，rook-ceph-block 10G。

### 4.2 清单文件
`d:\Agent Work Space\6a9e9ea1b1520745e24f3b87\rabbitmq-dev-pvc.yaml`

### 4.3 核心配置
- 镜像：`rabbitmq:3.13-management`
- 认证：Secret `rabbitmq-credential-secret`，账号密码 `rabbitmq123456 / rabbitmq123456`
- 存储：rook-ceph-block PVC `10Gi`（`rabbitmq-data`，已 Bound）
- 资源限制：req `250m/512Mi` → lim `1C/1Gi`
- `securityContext.fsGroup: 998`（RabbitMQ 默认非 root 用户 uid 998，解决 rook-ceph CSI 卷写权限）
- 暴露：AMQP NodePort `31672`、Management NodePort `31673`

### 4.4 验证结果
- Pod `1/1 Running`，日志 `Server startup complete`、用户权限设置成功
- Management UI (31673)：HTTP 200
- Management API：带认证 HTTP 200
- AMQP 31672：端口开放

### 4.5 访问方式
- UI：`http://节点IP:31673`
- AMQP：`amqp://rabbitmq123456:rabbitmq123456@节点IP:31672`

---

## 五、Milvus + MinIO + etcd 部署

### 5.1 需求
- 在 dev 命名空间安装 Milvus 与 MinIO，NodePort 对外暴露 API/UI
- 限制 CPU/内存；Milvus 与 MinIO 各 rook-ceph-block 30G
- MinIO 需同时向外提供服务并向 Milvus 提供服务
- Milvus 所需的 etcd **不对外暴露**，仅集群内部提供服务，rook-ceph-block 5G

### 5.2 清单文件
`d:\Agent Work Space\6a9e9ea1b1520745e24f3b87\milvus-minio-dev-pvc.yaml`

### 5.3 核心配置

| 组件 | 镜像 | 存储 | 资源限制 | 暴露方式 |
|------|------|------|----------|----------|
| etcd | quay.io/coreos/etcd:v3.5.25 | 5Gi | req 100m/256Mi → lim 1C/512Mi | ClusterIP 内部 |
| MinIO | minio/minio:RELEASE.2024-12-18T13-15-44Z | 30Gi | req 200m/512Mi → lim 1C/1Gi | NodePort 31000/31001 |
| Milvus | milvusdb/milvus:v2.6.18 | 30Gi | req 500m/1Gi → lim 2C/2Gi | NodePort 30530/30091 |

### 5.4 架构要点
- **etcd**：Type=ClusterIP（无 nodePort），仅供 Milvus 通过 `milvus-etcd-dev:2379` 内部访问
- **MinIO 单实例双角色**：一个 NodePort Service，ClusterIP 供 Milvus（`milvus-minio-dev:9000`），nodePort 供外部（API 31000 / UI 31001）
- **Milvus**：standalone 模式，使用外部 etcd + MinIO，`ETCD_USE_EMBED=false`
- MinIO 使用 `securityContext.fsGroup: 10001`（该版镜像默认非 root 用户 uid 10001，解决 CSI 卷权限）

### 5.5 依赖注入（Milvus 环境变量）
- `MINIO_ADDRESS=milvus-minio-dev:9000`
- `MINIO_BUCKET_NAME=milvus-bucket`、`MINIO_SECURE=false`
- `ETCD_ENDPOINTS=milvus-etcd-dev:2379`、`ETCD_USE_EMBED=false`、`ETCD_ROOT_PATH=/milvus/dev`
- 凭证由 MinIO Secret 注入（access/minio123456，secret/miniosec123456）

### 5.6 验证结果
- 三个 PVC 全部 Bound（etcd 5Gi / minio 30Gi / milvus 30Gi）
- 三个 Pod 全部 `1/1 Running`
- Milvus 日志：`Service registered successfully` / `Proxy successfully started`，session 键已写入 etcd（证明 Milvus→etcd→MinIO 内部集成正常）
- 集群内 Milvus 经 svc DNS 访问 MinIO 健康检查返回 200

### 5.7 访问方式（节点 IP 192.168.254.129）
- MinIO API：`http://192.168.254.129:31000`（`minio123456 / miniosec123456`）— 健康检查 200
- MinIO UI：`http://192.168.254.129:31001` — 200
- Milvus API（gRPC）：`192.168.254.129:30530`（`root / milvus123456`）— 端口开放
- Milvus WebUI：`http://192.168.254.129:30091/webui/` — 200（**注意：需带 `/webui/` 路径，根路径 `/` 返回 404**）
- etcd：仅集群内部 `milvus-etcd-dev:2379`，不对外

### 5.8 etcd 对外开放的排查结论
测试 `192.168.254.129:2379` 时 TCP 连接成功，曾疑为误暴露。核查后确认：该端口链路连接的是 **master 节点自身的 kubeadm 控制平面 etcd（静态 Pod 监听在 master 本地 2379）**，并非 dev 中 Milvus etcd。deployed etcd Service 类型为 ClusterIP 且无 nodePort，Pod 位于 node2，确实仅集群内部。

---

## 六、会话经验沉淀

1. **ES 8.x 密码机制**：`ELASTIC_PASSWORD` 只设 elastic 用户；`kibana_system` 等内置用户需手动用 `_security/user/<user>/_password` API 改密
2. **PowerShell 嵌套引号**：`kubectl exec ... bash -c 'curl ... -d "{\"k\":\"v\"}"'` 在 PowerShell 下易丢 JSON，稳妥做法是"写脚本 → `kubectl cp` → 容器内执行"
3. **rook-ceph CSI 卷权限**：各镜像以非 root 用户运行时，需在 Deployment 设 `securityContext.fsGroup`（数值 = 镜像运行用户的 uid）：
   - ES/Kibana → 1000
   - RabbitMQ → 998
   - MinIO（2024 版） → 10001
   - etcd / Milvus（root 运行）→ 无需设置
4. **Milvus WebUI 路径**：需 `/webui/` 而非 `/`
5. **镜像源**：docker.io → `docker.1ms.run`；quay.io、docker.elastic.co 直连；`bitnami/kafka` 在 1ms 源不可用
6. **NodePort 端口规划**：ES 31200/31202、Kibana 31201、RabbitMQ 31672/31673、MinIO 31000/31001、Milvus 30530/30091、Redis 30379

---

## 七、最终资源清单汇总（dev 命名空间）

| 资源 | 状态 | 关键配置 |
|------|------|----------|
| redis-dev | Running | StatefulSet，NodePort 30379 |
| elasticsearch-dev | Running | NodePort 31200/31202，30G |
| kibana-dev | Running | NodePort 31201，5G |
| rabbitmq-dev | Running | NodePort 31672/31673，10G |
| milvus-etcd-dev | Running | ClusterIP 内部，5G |
| milvus-minio-dev | Running | NodePort 31000/31001，30G |
| milvus-dev | Running | NodePort 30530/30091，30G |